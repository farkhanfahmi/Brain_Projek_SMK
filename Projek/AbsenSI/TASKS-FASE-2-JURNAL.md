---
tags: [absensi, tasks, fase-2, jurnal]
updated: 2026-07-23
---

# AbsenSI — Task Breakdown Fase 2: Dashboard Guru — Jurnal Mengajar & Absensi Mapel

← [[Projek/AbsenSI/00-INDEX AbsenSI|Index]]

> Spec lengkap: [[Projek/AbsenSI/06-Features/dashboard-guru-jurnal|dashboard-guru-jurnal.md]] — **baca dulu sebelum eksekusi task apapun di bawah**, banyak keputusan desain (kenapa gating berurutan begitu, kenapa mode blok global bukan per-kelas, dll) hanya dijelaskan lengkap di sana, task file sengaja tidak mengulang semuanya.
>
> **Status spec saat breakdown ini dibuat: masih ada open question yang belum final** (lihat bagian "Open Questions" di `dashboard-guru-jurnal.md`) — task di bawah ditulis berdasarkan keputusan yang SUDAH firm dari interview. Kalau saat eksekusi menemukan area yang ternyata menyentuh open question yang belum diputuskan, STOP dan konfirmasi ke user dulu, jangan berasumsi.
>
> **Eksekusi berurutan.** Urutan T038→T053 dirancang linear: schema dulu, lalu backend per-domain (resolver jadwal → izin → sesi mengajar → jurnal), baru frontend (guru dulu, admin_jurnal terakhir). Jangan loncat kecuali dependency "Depends on" di task terkait sudah selesai semua.
>
> **Catatan penomoran (2026-07-22):** T054, T055, T056 ditambahkan belakangan (fitur semester + loopback tahun ajaran + revisi mekanisme blok A/B) — nomornya lebih besar dari T038-T051 yang mereka pengaruhi, tapi **urutan eksekusi sebenarnya mengikuti "Depends on" di tiap file, bukan urutan angka**. T054 harus selesai SEBELUM T039 dan T047. Ikuti urutan blok di bawah, bukan urutan nomor.
>
> **⚠️ Revisi besar 2026-07-22 (baca sebelum eksekusi Blok 0):** mekanisme mode blok A/B DIROMBAK TOTAL. Draf awal T038/T039 merancang "1 titik acuan tanggal + hitung selisih minggu otomatis" (`schedule_config.minggu_acuan_tanggal`/`minggu_acuan_nilai`) — **mekanisme ini DIBATALKAN sepenuhnya** untuk menghindari kesalahan sistem dari perhitungan otomatis. Diganti: admin (`admin_jurnal`) input RENTANG TANGGAL EKSPLISIT untuk tiap minggu A dan B, per semester, lewat kalender visual (T056). Konsekuensinya:
> - `schedule_config` (T038) sekarang HANYA berisi `toleransi_terlambat_menit` — field mode/minggu acuan sudah dihapus dari sana
> - Field `mode` (blok/normal) pindah ke level `semesters` (T054) — per-semester, bukan konfigurasi sekolah global
> - Tabel baru `block_week_ranges` (T054) menyimpan rentang tanggal eksplisit per minggu A/B
> - `ScheduleResolverService.getMingguAktif()` (T039) sekarang PURE LOOKUP ke `block_week_ranges`, tidak ada aritmetika sama sekali
> - Validasi ketat WAJIB di T054: tidak boleh ada celah (gap) atau tumpang tindih (overlap) tanggal — baik dalam 1 semester maupun ANTAR semester (termasuk semester yang belum aktif tapi sedang disiapkan)
> - Celah tanggal yang belum admin lengkapi → **hard block total**, semua sesi "Mulai Mengajar" terkunci untuk tanggal itu sampai admin melengkapi
> - Semester Genap BOLEH disiapkan/diinput sebelum Semester Ganjil selesai (operasional), TAPI tanggalnya tetap tidak boleh tumpang tindih secara kalender dengan semester lain manapun (validasi real-time saat submit)
>
> Kalau Anda membaca versi task file yang masih menyebut "minggu_acuan" atau "hitung selisih minggu" — itu SUDAH USANG, jangan ikuti. Rujuk isi T038/T039/T054 versi terkini.

---

## 🟢 STATUS TERKINI (2026-07-22) — BACA INI DULU

**Ditemukan lewat pengecekan langsung ke working tree (bukan asumsi):** T038–T054 dan T056 **SUDAH DIEKSEKUSI** (kode backend `apps/api/src/{teaching-sessions,semesters,block-week-ranges,teacher-permits,schedule-resolver}` dan frontend `apps/web/src/app/{(guru),(admin-jurnal)}` sudah ada) — tapi **BELUM ADA SATU PUN YANG TER-COMMIT** (`git status` menunjukkan semuanya masih `??`/modified di working tree). Kalau Anda pindah sesi/mesin tanpa commit dulu, risiko kehilangan pekerjaan ini nyata — pertimbangkan commit sebelum lanjut ke task baru manapun.

| Task | Status nyata |
|---|---|
| T038, T039, T040, T041, T042, T043, T044, T045, T046, T047 | ✅ Kode ada di working tree (belum commit) |
| T048, T049 | ✅ Kode ada (`(guru)/guru/jurnal`, `(guru)/guru/sesi`, `(guru)/guru/izin`) |
| T050, T051 | ✅ Kode ada (`(admin-jurnal)/admin-jurnal/{toleransi,mapel,jadwal,izin}`) |
| T052, T053 | ✅ Kode ada (`(admin-jurnal)/admin-jurnal/wali-kelas`, `(guru)/guru/wali-kelas`) |
| T054, T056 | ✅ Kode ada (`apps/api/src/semesters`, `block-week-ranges`; `(admin-jurnal)/admin-jurnal/{semester,jadwal-blok}`) |
| **T055** | ❌ **BELUM dieksekusi** — tidak ditemukan perubahan di modul Rekap Admin Fase 1 untuk filter tahun ajaran/semester |
| T057, T058, T059 | ✅ Kode ada (belum commit) — token status warna + `packages/ui/src/components/{data-table,activity-feed}` (verifikasi visual Playwright: badge shipped/processing cocok hex spec, scroll activity feed clip di maxHeight) |
| **T060** | ❌ **BELUM dieksekusi** — audit visual menyeluruh (lihat Blok 9) |
| T061, T063 | ✅ Kode ada (belum commit) — schema biodata guru + wali murid siswa, T061 terverifikasi live di DB via MySQL MCP |
| **T062** | ❌ **BELUM dieksekusi** — ETL script belum ada file-nya sama sekali (lihat Blok 10) |
| T064 | ✅ Kode ada (belum commit) — primitif `Sheet`+`Tabs` di `packages/ui/src/components/ui/{sheet,tabs}.tsx` (lihat Blok 11) |
| T065 | ✅ Kode ada (belum commit) — `guru-view.tsx` migrasi ke Sheet+Tabs 3 section, `PersonStatus` enum ditambah `cuti` (migrasi DB `20260722163645_add_cuti_to_person_status`, sudah diterapkan) |
| T066 | ✅ Kode ada (belum commit) — `siswa-view.tsx` migrasi ke Sheet+Tabs 4 section (Data Pokok/Biodata/Kontak & Alamat/Data Wali), logic kondisional Status→Alasan Nonaktif→Tahun Lulus terverifikasi Playwright, backend `CreateStudentDto`+`students.service.ts` diperluas (field T063 belum pernah di-wire ke DTO sebelumnya, sama seperti temuan T065 di Teacher) |
| T067 | ✅ **Diverifikasi lengkap 2026-07-23** — semua 14 controller di tabel spec dicek satu-per-satu (sebagian sudah log manual di service, tidak boleh ditambah decorator dobel); gap tambahan `students.controller.ts` (`POST`/`PATCH`) ditemukan+diperbaiki; `tsc`+`jest`(171/171)+`nest build` bersih, diverifikasi live via API+MySQL |
| T068, T069, T070 | ✅ Kode ada (belum commit) — TV Piket LENGKAP: schema `tv_sessions` + `TvPiketGuard` + module `tv-sessions`/`tv-piket` (backend), route `/tv-piket/[kampusId]` + hooks (`use-tv-piket-data`, `use-auto-scroll`, `tv-piket-init`) + proxy API (frontend). Diverifikasi end-to-end via curl+Playwright: create/revoke token, 401 token invalid, 403 role salah, token tidak pernah bocor di list/revoke response, `@LogActivity` tercatat+redacted di `activity_log`, data ter-scope kampus benar, angka persentase cocok Rekap Admin, reload tanpa `?token=` tetap jalan (localStorage), auto-scroll teruji dengan data sintetis 25 baris (scroll turun→naik→ulang), Socket.IO TV Piket connect+auto-join kampus room terkonfirmasi di log server. |
| T071 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — `tinggal_kelas_pada` (nullable date) di `Student`, `GET /kelas` diperluas `jumlahSiswa` (hanya status aktif), diverifikasi live cocok manual COUNT via MySQL |
| T075 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — kolom `kelas.tingkat` (enum X/XI/XII, NOT NULL) via migrasi 2 tahap + script backfill permanen (`apps/api/scripts/backfill-tingkat-kelas.ts`), dropdown Tingkat di form Kelas, diverifikasi live (POST tanpa tingkat → 400, dengan tingkat → 201) |
| T072 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — `POST /students/validate-nisn-batch` + `POST /kelas/:id/plot-siswa` (race-condition-safe, `@LogActivity`), UI kolom Jumlah Siswa + tombol Plot Siswa di tabel Kelas, halaman `/kelas/[id]/plot-siswa` 2 kolom (paste-NISN debounced 500ms + `StatusBadge` preview, list akumulatif kanan). **Blocker skema ditemukan+diperbaiki (user confirm):** `Student.kelasId` semula `NOT NULL` — kontradiksi langsung dengan premis T072 (siswa belum ditempatkan = `kelasId` null). Migrasi `20260723125329_make_student_kelas_id_nullable` (kelasId+relasi `kelas` jadi nullable), 14 TS18047 di 5 file diperbaiki (assertion `!` utk implicit-non-null via `where` filter, optional-chaining `?.` utk authorization check). **Gap kedua ditemukan+diperbaiki (user confirm):** setelah kelasId nullable, TERNYATA tidak ada satupun jalur produksi yang bisa menghasilkan siswa `kelasId: null` — `CreateStudentDto.kelasId` masih wajib (`@IsInt`, bukan optional) dan `import.service.ts` selalu match+wajib kelas/jurusan. Diperbaiki: `kelasId` jadi opsional di create-student DTO+service, kolom `kelas`/`jurusan` di CSV import jadi opsional (kosong berdua = siswa baru tanpa kelas; kalau salah satu diisi wajib keduanya match kelas existing). `tsc`+`jest`(171/171) bersih di kedua titik. Diverifikasi live end-to-end: curl 3 NISN campuran (valid/sudah_ada_kelas_lain/tidak_ditemukan) → badge status benar, plot sukses → `kelas_id` ter-update via MySQL, retry sama → ditolak race-condition, `activity_log` `student.plot_kelas` tercatat utk sukses MAUPUN gagal. Full production build (`pnpm turbo run build`) bersih 6/6 task, termasuk route baru `/kelas/[id]/plot-siswa`. Data test dibersihkan setelah verifikasi. |
| T073 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — `POST /kelas/kenaikan-massal`: 1 database transaction (`prisma.$transaction`, all-or-nothing) memproses semua kelas sekaligus — item `lulus: true` (validasi backend WAJIB `tingkat: XII`, ditolak 400 kalau bukan) set siswa aktif `status: nonaktif`+`alasanNonaktif: lulus`+`tahunLulus`, item `kelasTujuanId` pindahkan siswa aktif ke kelas tujuan, `nisnTinggalKelas` per-item exclude siswa dari proses + set `tinggalKelasPada`. `@LogActivity` manual (bukan decorator — tidak ada `:id` tunggal untuk snapshot otomatis, pola sama `ScheduleConfigService`), `action: kelas.kenaikan_massal`, `targetType: academic_year` (pakai `tahunAjaranTujuanId`), snapshot `ringkasan` lengkap. **Keputusan desain dikonfirmasi user:** spec asli minta `tahunLulus` diparse dari `AcademicYear.nama` KALAU formatnya reliable — dicek data nyata, `nama` ternyata teks bebas (ada yang bukan `"YYYY/YYYY"` murni dari testing sebelumnya), parsing tidak reliable → dipilih field eksplisit terpisah `tahunLulus` di request (bukan auto-parse), sesuai opsi kedua spec. UI: halaman `/kelas/kenaikan-massal` (link dari halaman Kelas) — dropdown Tahun Ajaran Tujuan (disable semua sampai terisi), input Tahun Lulus eksplisit, tabel semua kelas dengan dropdown Kelas Tujuan per baris (kelas `XII` dapat opsi tambahan "Lulus" di atas daftar, kelas lain tidak — filter berdasar `tingkat` bukan visual saja), Popover "Atur" NISN Tinggal Kelas per baris (reuse `validate-nisn-batch` T072, tapi label di-reinterpretasi: NISN cuma dianggap "cocok" kalau `kelasLain` sama persis dengan nama kelas baris itu — status asli endpoint "valid"/lainnya diberi label beda karena makna T072 vs T073 berbeda), dialog konfirmasi ringkasan sebelum submit, warning ringan kalau ada kelas belum dipilih tujuannya (diskip, tidak blocking). Diverifikasi live end-to-end via curl+MySQL: batch campuran (1 kelas naik normal 2 siswa + 1 kelas XII lulus 1 siswa dengan 1 siswa tinggal kelas via `nisnTinggalKelas`) → semua state benar di DB (`kelas_id`, `status`/`alasan_nonaktif`/`tahun_lulus`, `tinggal_kelas_pada`); `lulus:true` untuk kelas `tingkat: X` → 400 ditolak dengan pesan jelas; transaction rollback dikonfirmasi via code review (`prisma.$transaction` interactive form — standard Prisma atomic rollback on throw, request dengan `kelasAsalId` tidak valid gagal total sebelum apapun ter-apply, dikonfirmasi state kelas tidak berubah); `activity_log` `kelas.kenaikan_massal` tercatat dengan `ringkasan` lengkap, TIDAK tercatat untuk request yang gagal validasi (sebelum masuk handler). `tsc`+`jest`(171/171) bersih, full `pnpm turbo run build` bersih 6/6 termasuk route baru `/kelas/kenaikan-massal`. Data test (2 kelas + 4 siswa) dibersihkan pasca-verifikasi. |
| T074 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — UI-only, TIDAK ada endpoint API baru (`PATCH /students/:id` existing sudah cukup, sesuai spec). `siswa-detail-view.tsx`: field Kelas jadi editable (klik nama kelas → dropdown Select semua kelas → `PATCH { kelasId }`, update state tanpa reload), tombol "Tandai Keluar" (Dialog kecil sesuai `03-components.md` ≤6 field — BUKAN Sheet) dengan dropdown Alasan (Lulus/Mengundurkan Diri/Lainnya) + field Tahun Lulus kondisional (muncul HANYA kalau alasan="lulus", logic sama pola T066), submit → `PATCH { status: nonaktif, alasanNonaktif, tahunLulus? }`. **Reversibility**: tombol "Aktifkan Kembali" muncul kalau `status === "nonaktif"` → `PATCH { status: "aktif" }` SAJA (TIDAK kirim `alasanNonaktif`/`tahunLulus` null — dicek dulu: `class-validator`'s `@IsOptional()` cuma skip `undefined`, bukan `null`, jadi kirim eksplisit `null` akan gagal validasi; field lama jadi stale tapi harmless karena cuma ditampilkan kondisional saat `status === "nonaktif"` — pola SAMA PERSIS sudah dipakai T066 form Tambah/Ubah Siswa, dikonfirmasi via baca kode `siswa-view.tsx`, bukan bug baru). Diverifikasi live via curl (endpoint yang sama dipanggil UI) + MySQL: (1) pindah kelas kelas 1→2 tersimpan benar, (2) Tandai Keluar Lulus+tahunLulus=2026 tersimpan benar, (3) Aktifkan Kembali → status aktif, field lama stale (lulus/2026) sesuai pola yang diharapkan, (4) Tandai Keluar Mengundurkan Diri TANPA tahunLulus → tersimpan benar, `tahunLulus` lama (2026) tetap stale tak berubah. `tsc` bersih, full `pnpm turbo run build` bersih 6/6 (`/siswa/[id]` route size naik 4.61kB→5.42kB mencerminkan UI baru). Data test dibersihkan pasca-verifikasi. |
| T076 | ✅ **Dieksekusi+diverifikasi 2026-07-23** — Direktori Siswa dashboard piket. **Backend**: `guru_piket` dibuka HANYA di `GET` (`students.controller.ts` — list+detail, `attendance.controller.ts` — riwayat-catatan, `kelas.controller.ts`+`jurusan.controller.ts` — dropdown filter), method mutasi (`POST`/`PATCH`/`DELETE`) TETAP eksklusif role admin (di-override per-`@Get()`, bukan class-level, supaya tidak ikut buka mutasi). Filter `nama` (search, `contains`) ditambah ke `ListStudentsDto`+`findAll()` — belum ada sebelumnya. Scope LINTAS KAMPUS SENGAJA (ADR-015 exception, dicatat eksplisit di komentar kode servis+view — bukan bug untuk "diperbaiki" nanti). **Blocker arsitektur ditemukan+diperbaiki (user confirm):** `(admin)/layout.tsx` redirect UNCONDITIONAL semua `guru_piket` ke `/piket` sebelum child render — `/siswa/[id]` (halaman detail existing yang WAJIB di-reuse per spec) sama sekali tidak bisa diakses piket. Next.js App Router layout Server Component tidak otomatis dapat pathname — ditambah header `x-pathname` di `middleware.ts` (3 titik `NextResponse.next()`/redirect diperbaiki supaya header konsisten terbawa), `AdminLayout` baca header itu dan izinkan `guru_piket` lewat KHUSUS kalau pathname match regex `/^\/siswa\/\d+$/` (bukan `/siswa` list, bukan route admin lain). **Keputusan scope dikonfirmasi user:** shell Sidebar/AdminContent TIDAK diganti PiketSidebar/PiketContent untuk exception path ini (dibiarkan render sidebar admin biasa) — item lain tetap redirect balik kalau diklik + backend tetap 403, dianggap cukup untuk 1 halaman read-only, tidak menambah coupling PiketDutyProvider ke AdminLayout. **UI**: `siswa-detail-view.tsx` terima prop `readOnly` (dihitung server-side dari role di `page.tsx`, bukan client context — belum ada client user-context di codebase) — sembunyikan tombol edit kelas/upload foto/hapus foto/Tandai Keluar/Aktifkan Kembali, link "Kembali" arah ke `/piket/siswa` bukan `/siswa`; fetch `GET /kelas` (dipakai dropdown Pindah Kelas admin) di-skip total kalau `guru_piket` (endpoint 403 utk role itu, tidak dibutuhkan view read-only, akan crash kalau tetap di-fetch). Halaman baru `/piket/siswa`: search nama debounced 400ms + filter Kelas/Jurusan (client-side, pola sama T072), baris `DataTableEntityCell` (foto+nama) + kelas + kampus, klik baris → `/siswa/[id]`. Menu "Direktori Siswa" ditambah ke `piket-sidebar.tsx`. Diverifikasi live via curl (login akun `guru_piket` nyata `piket1`, password di-reset via admin karena hash lama tidak diketahui) + MySQL: (1) `GET /students?nama=Budi` dari akun kampus 1 nemuin siswa test di kampus 2 — cross-kampus terbukti jalan, (2) `GET /students/:id` 200, (3) `riwayat-catatan` 200, (4) `GET /kelas` 200, (5) `GET /jurusan` 200, (6) `POST /students` 403, (7) `PATCH /students/:id` 403 — SEMUA 7 acceptance criteria eksplisit di spec terverifikasi. `tsc`+`jest`(171/171) bersih di backend, `tsc` bersih di frontend, full `pnpm turbo run build` bersih 6/6 termasuk route baru `/piket/siswa`. Data test dibersihkan pasca-verifikasi. **CATATAN OPERASIONAL:** password akun `piket1` di DB sudah BERUBAH akibat reset saat verifikasi ini (password lama tidak diketahui) — kalau ada yang perlu login manual sebagai piket1, minta reset ulang via menu Akun. |

> ✅ **2 bug ditemukan & DIPERBAIKI saat verifikasi T064/T065 (di luar scope asal, tapi sudah dieksekusi):**
> 1. `Dialog`/`Sheet` render dengan background beige page (`#EEE6D9`), bukan putih. Root cause: className `bg-background` resolve ke CSS var `--background` (page), bukan `--card`/`bg-surface` (putih). Fix: ganti semua `bg-background` → `bg-surface` di `dialog.tsx`, `sheet.tsx`, `button.tsx` (varian outline), `tabs.tsx` (trigger tidak aktif).
> 2. `shadow-card`/`shadow-popover` render sebagai glow PUTIH (bukan shadow gelap) — root cause GANDA: (a) `hsl(var(--color-shadow) / alpha)` tidak ter-resolve browser dengan benar kalau disimpan di custom property `--tw-shadow` (harus pakai `rgba(23,20,18,x)` literal), DAN (b) nama key `card`/`popover` di `boxShadow` collide dengan `colors.card`/`colors.popover` — Tailwind auto-generate utility `shadow-{colorName}` yang override dengan urutan CSS menang, selalu putih. Fix: rename key jadi `elevated`/`elevated-hover`/`overlay` (bukan collide dengan `colors`), pakai `rgba()` literal. **Class Tailwind BERUBAH**: `shadow-card`→`shadow-elevated`, `shadow-popover`→`shadow-overlay` (sudah di-replace di 53 file). Verifikasi Playwright: computed `box-shadow` sekarang `rgba(23, 20, 18, x)`, bukan putih.
>
> ⚠️ **Keputusan user (2026-07-22):** `PersonStatus` diperluas dengan value `cuti` (dulu cuma `aktif`/`nonaktif`) untuk dropdown Status guru di T065. Enum ini SHARED dengan `Student` — semua query existing pakai `where: { status: "aktif" }` eksplisit (bukan switch exhaustive), jadi `cuti` otomatis ter-exclude dengan benar tanpa perlu ubah logic lain. Migrasi: `20260722163645_add_cuti_to_person_status`.

**Yang PALING PENTING untuk sesi berikutnya:** T038–T054/T056 sudah ada kode-nya tapi **belum pernah diverifikasi visual nyata** (dijalankan di browser, di-screenshot) — itulah kenapa T060 (audit visual) jadi prioritas berikutnya, BUKAN mulai dari T055 begitu saja. Baca Blok 9 di bawah.

> **Update 2026-07-23 (sesi lanjutan):** T070 (UI TV Piket) sudah dieksekusi — Blok 13 (TV Piket, T068-T070) sekarang 3/3 LENGKAP. 2 bug lagi ditemukan+diperbaiki saat verifikasi (di luar scope asal task, tapi sudah dieksekusi): (1) `TvSessionsService.revoke()` awalnya balikin token mentah di response (harusnya cuma sekali saat create), (2) widget persentase TV Piket (T069) awalnya filter siswa `status: "aktif"` padahal Rekap Admin Fase 1 tidak filter — bikin angka TV tidak cocok dengan Rekap (melanggar acceptance criteria "1 sumber kebenaran"), sudah disamakan. Detail lengkap di Blok 13 bawah.
>
> **Update lanjutan 2026-07-23:** T067 (Kelengkapan Activity Log) diverifikasi lengkap 1-per-1 seluruh 14 controller — gap tambahan `students.controller.ts` ditemukan+diperbaiki di luar tabel asli. T071 (schema Tinggal Kelas + jumlahSiswa) dan T075 (schema Tingkat Kelas, migrasi 2 tahap + backfill) dieksekusi+diverifikasi live. T055, T060, T062, dan T073-T074/T076 MASIH BELUM ada kode sama sekali.
>
> **Update lanjutan 2026-07-23 (2):** T072 (Penempatan Siswa Baru via Paste-NISN) dieksekusi+diverifikasi lengkap end-to-end. 2 gap arsitektur ditemukan MID-TASK dan diperbaiki dengan konfirmasi eksplisit user (bukan diasumsikan) — lihat detail di baris T072 tabel atas: (1) `Student.kelasId` semula `NOT NULL`, kontradiksi langsung dengan premis fitur; (2) setelah difix, ternyata tidak ada jalur produksi (create/import) yang bisa menghasilkan siswa `kelasId: null`, jadi fitur Plot Siswa tidak ada gunanya tanpa siswa unplaced untuk diplot — kedua DTO/service dilonggarkan. Blok 14 sekarang 3/5.
>
> **Update lanjutan 2026-07-23 (3):** T073 (Kenaikan Kelas Massal) dieksekusi+diverifikasi lengkap end-to-end — 1 database transaction utk semua kelas, validasi backend `lulus:true` HANYA kelas XII, `@LogActivity` manual (tidak ada `:id` tunggal utk decorator otomatis). 1 keputusan desain dikonfirmasi user: `tahunLulus` pakai field eksplisit di request (BUKAN auto-parse dari `AcademicYear.nama`) karena format nama di data nyata tidak reliable utk diparse — sesuai fallback yang memang disediakan spec asli. Blok 14 sekarang 4/5, tinggal T074.
>
> **Update lanjutan 2026-07-23 (4):** T073 selesai, langsung lanjut T074 (Pindah Kelas Individual + Tandai Siswa Keluar) — UI-only di `siswa-detail-view.tsx`, TIDAK ada endpoint baru (pakai `PATCH /students/:id` existing). Blok 14 SEKARANG 5/5 LENGKAP (T071-T075 semua selesai). Sisa Blok 15 (T076) untuk Fase 2.
>
> **Update lanjutan 2026-07-23 (5):** T076 (Direktori Siswa Dashboard Piket) dieksekusi+diverifikasi lengkap — Blok 15 SEKARANG LENGKAP 1/1. 1 blocker arsitektur signifikan ditemukan+diperbaiki dengan konfirmasi eksplisit user: `(admin)/layout.tsx` redirect semua `guru_piket` unconditional sebelum halaman detail siswa (yang WAJIB di-reuse per spec T076, bukan dibuat ulang) sempat render — solusinya nambah mekanisme baru (header `x-pathname` dari middleware ke layout) yang scope-nya sengaja dibatasi HANYA untuk 1 pathname regex, bukan buka semua akses admin. **Fase 2 sekarang TUNTAS kecuali 3 task: T055 (Loopback Rekap), T060 (Audit Visual), T062 (ETL Migrasi Lama)** — total 36/39.

---

## 📊 Progress

| Blok | Task | Selesai |
|---|---|---|
| 0 — Schema & Resolver Jadwal | T038–T039, T054 | 3/3 (kode ada, belum commit) |
| 1 — Job Sesi & Config | T040, T042, T044 | 3/3 (kode ada, belum commit) |
| 2 — API Inti Sesi Mengajar | T041, T043, T045 | 3/3 (kode ada, belum commit) |
| 3 — API Izin Guru & RBAC Admin Jurnal | T046–T047 | 2/2 (kode ada, belum commit) |
| 4 — UI Dashboard Guru | T048–T049 | 2/2 (kode ada, belum commit) |
| 5 — UI Dashboard Admin Jurnal | T050–T051 | 2/2 (kode ada, belum commit) |
| 6 — Wali Kelas | T052–T053 | 2/2 (kode ada, belum commit) |
| 7 — Loopback Rekap | T055 | **0/1 — BELUM DIKERJAKAN** |
| 8 — UI Kalender Blok Minggu | T056 | 1/1 (kode ada, belum commit) |
| 9 — Perbaikan UI dari Audit Referensi EzMart | T057–T060 | 3/4 (kode ada, belum commit) — T060 belum dikerjakan |
| 10 — Migrasi Database Lama | T061–T063 | **3/3 LENGKAP** — T062 (ETL) dieksekusi+diverifikasi 2026-07-24 (fix bug `tingkat` wajib, lihat catatan blok). **BUKAN cutover final** — sistem lama masih live, akan ada re-sync dari dump baru saat go-live |
| 11 — Form Input Modern (Sheet+Tabs) | T064–T066 | 3/3 (kode ada, belum commit) |
| 12 — Kelengkapan Activity Log | T067 | 1/1 (diverifikasi lengkap 2026-07-23, termasuk gap tambahan `students.controller.ts`) |
| 13 — TV Piket | T068–T070 | 3/3 (kode ada, belum commit) |
| 14 — Plotting Siswa ke Kelas | T071–T075 | **5/5 LENGKAP** (T071-T075 semua dieksekusi+diverifikasi 2026-07-23) |
| 15 — Direktori Siswa Dashboard Piket | T076 | **1/1 LENGKAP** (dieksekusi+diverifikasi 2026-07-23) |
| 16 — Polish Admin: Guru/Kartu/Foto | T077–T079 | **3/3 LENGKAP** (T077-T079 semua dieksekusi+diverifikasi 2026-07-24) |
| 17 — Pagination, PKL, Akun Piket, Kalender 12-Bulan, Rename Mesin | T080–T084 | **5/5 LENGKAP** (T080-T084 semua dieksekusi+diverifikasi) |
| 18 — Perbaikan Mesin/Kiosk (Deteksi IP, CRUD) | T085–T086 | **2/2 LENGKAP** (T085, T086 dieksekusi+diverifikasi 2026-07-26) |
| 19 — TTL JWT Per-Role (Guru/Kepsek Panjang, Piket Jam 18:00) | T087 | **1/1 LENGKAP** (dieksekusi+diverifikasi 2026-07-26) |
| 20 — Migrasi UUID Tabel Sensitif (TIDAK MENDESAK) | T088 | 0/1 — TUNGGU instruksi eksplisit user, jangan prioritaskan |
| 21 — Redesign Tampilan Absensi Kiosk + Watermark | T089 | **1/1 LENGKAP** (dieksekusi+diverifikasi live 2026-07-26, termasuk fix bug kontras avatar) |
| **Total** | | **49/52** kode ada/diverifikasi (mayoritas belum commit) · **2/52 murni belum dikerjakan** *(T055, T060 — dikoreksi 2026-07-26, T086 & T089 SELESAI, Blok 18 & 21 LENGKAP; T088 sengaja ditunda, belum instruksi eksplisit user)* |

> "Selesai" di atas = kode SUDAH DITULIS, BUKAN berarti sudah diverifikasi jalan/benar. Verifikasi nyata adalah tujuan T060.

---

## 🏗️ Blok 0 — Schema & Resolver Jadwal
> Fondasi seluruh fitur. Semua task lain depend langsung/tidak langsung ke blok ini.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T038-schema-jurnal-mengajar|T038 — Prisma Schema: Jurnal Mengajar, Izin Guru, Geofence]] *(field mode/minggu-acuan sudah dihapus dari task ini, lihat catatan revisi di atas)*
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T054-schema-semester|T054 — Schema: Semester + Rentang Blok A/B Eksplisit]] *(kerjakan SEBELUM T039 meski nomornya besar — T039 depend ke task ini, task PALING SENSITIF di blok ini karena berisi validasi no-gap/no-overlap)*
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T039-resolver-jadwal-aktif|T039 — Service Resolver: Jadwal Aktif Hari Ini (Lookup Rentang Blok A/B)]] *(direvisi total — sekarang pure lookup, bukan hitung selisih minggu)*

## 🏗️ Blok 1 — Job Sesi & Config
> Job harian generate sesi, config toleransi keterlambatan, job auto-close. Bisa dikerjakan agak paralel setelah Blok 0, tapi T044 depend ke T043 (Blok 2) jadi urutan penomoran tetap dijaga.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T040-job-generate-teaching-sessions|T040 — Job Harian: Generate teaching_sessions]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T042-toleransi-keterlambatan-config|T042 — Config Toleransi Keterlambatan Mengajar]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T044-job-auto-close-sesi|T044 — Job Auto-Close Sesi & Flag Follow-Up Izin]] *(depend ke T043, kerjakan setelah Blok 2)*

## 🏗️ Blok 2 — API Inti Sesi Mengajar
> Endpoint yang dipakai langsung oleh dashboard guru: lihat jadwal hari ini, mulai mengajar (dengan gating tap gerbang + waktu + geofence), isi jurnal & koreksi presensi.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T041-api-jadwal-guru-hari-ini|T041 — API: Jadwal Guru Hari Ini]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T043-start-session-geofence|T043 — API: Mulai Mengajar (Start Session) dengan Validasi Geofence]] ⚠️ *task paling sensitif — baca urutan validasi di spec dengan teliti*
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T045-api-jurnal-dan-presensi-kelas|T045 — API: Jurnal Mengajar & Presensi Kelas]]

## 🏗️ Blok 3 — API Izin Guru & RBAC Admin Jurnal
> Alur izin 2-tahap (admin approve dulu, baru guru submit tugas) + penegakan role `admin_jurnal` terkunci ke domain jurnal.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T046-api-izin-guru|T046 — API: Izin Guru Tidak Mengajar]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T047-rbac-admin-jurnal-mapel-jadwal|T047 — RBAC admin_jurnal + API Kelola Mapel & Jadwal Mengajar]]

## 🏗️ Blok 4 — UI Dashboard Guru
> Landing page guru (jadwal hari ini + tombol Mulai Mengajar/Izin) dan halaman detail sesi (jurnal + presensi + form tugas titipan).

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T048-ui-dashboard-guru-halaman-utama|T048 — UI: Dashboard Guru — Halaman Utama]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T049-ui-halaman-sesi-jurnal-presensi|T049 — UI: Halaman Sesi — Jurnal, Presensi Kelas & Form Izin]]

## 🏗️ Blok 5 — UI Dashboard Admin Jurnal
> Dashboard baru untuk role `admin_jurnal`: toleransi keterlambatan, kelola mapel & jadwal mengajar (per semester), kelola izin guru.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T050-ui-dashboard-admin-jurnal-jadwal|T050 — UI: Dashboard Admin Jurnal — Mapel, Jadwal & Toleransi Keterlambatan]] *(halaman "Konfigurasi Mode Blok" versi lama DIHAPUS dari task ini, lihat T056)*
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T051-ui-admin-jurnal-izin-guru|T051 — UI: Dashboard Admin Jurnal — Kelola Izin Guru]]

## 🏗️ Blok 6 — Wali Kelas
> Final (2026-07-21) — bukan role baru, extend akun `guru` dengan `kelas_id_wali` (pola identik `guru_piket.kampus_id`). Read-only murni. v1 scope: rekap kehadiran + rekap per mapel + catatan/kendala saja (bukan jurnal lengkap materi/tujuan/tugas — itu sudah dicatat sebagai perluasan masa depan di spec, tinggal buka filter kalau dibutuhkan nanti).

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T052-schema-dan-assign-wali-kelas|T052 — Schema kelas_id_wali + UI Admin Jurnal: Assign Wali Kelas]]
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T053-ui-menu-wali-kelas-guru|T053 — UI: Menu Wali Kelas di Dashboard Guru]]

## 🏗️ Blok 7 — Loopback Rekap
> Filter tahun ajaran/semester di halaman Rekap Admin existing (Fase 1) — data historis sudah ada, ini murni tambahan UI+filter.

- [ ] [[Projek/AbsenSI/06-Features/tasks/T055-rekap-loopback-tahun-ajaran|T055 — Rekap Kehadiran: Filter Loopback Tahun Ajaran & Semester]]

## 🏗️ Blok 8 — UI Kalender Blok Minggu
> Kalender visual untuk admin_jurnal menandai tiap minggu sebagai A/B — MENGGANTIKAN form input tanggal manual, mengurangi risiko human error untuk belasan pasangan rentang per semester.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T056-ui-kalender-blok-minggu|T056 — UI: Kalender Visual Input Rentang Blok Minggu A/B]]

## 🏗️ Blok 9 — Perbaikan UI dari Audit Referensi EzMart (BARU, 2026-07-22 — T057-T059 dieksekusi 2026-07-22, T060 masih tersisa)
> User menunjukkan 2 gambar referensi Figma asli EzMart yang jadi dasar `design-system/*.md`. Audit manual (baca gambar langsung) menemukan `03-components.md` sudah akurat untuk 1 gambar tapi gambar KEDUA (dashboard dengan tabel "Recent Orders" + "Recent Activity") belum pernah didokumentasikan — 4 komponen hilang, dan status badge tabel ternyata butuh 4 kategori warna (bukan cuma success/danger). Dokumen desain sudah diperbaiki (`01-colors.md`, `03-components.md`, `DESIGN.md`); blok ini adalah TASK EKSEKUSI dari perbaikan tersebut. **Kerjakan T057 dulu (fondasi token), lalu T058/T059 bisa paralel, T060 (audit visual) terakhir sebagai penutup — idealnya T060 dikerjakan setelah T057-T059 SEKALIGUS setelah rekonsiliasi status "kode ada tapi belum commit" di atas (audit percuma kalau kode yang diaudit belum stabil).**

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T057-tambah-token-status-workflow|T057 — Tambah Token Warna Status Workflow (Shipped/Processing) ke Tailwind Config]] — token ditambahkan di `packages/ui/src/globals.css` + `packages/config/tailwind.config.ts` (grup `status.shipped`/`status.processing`), komentar path vault diperbaiki. Verifikasi: computed style browser cocok persis `#FDECD1`/`#B8720A`/`#EDE3F7`/`#7C4FC7`.
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T058-komponen-data-table-generik|T058 — Komponen Generik: Data Table dengan Status Badge Workflow]] *(depend T057)* — dibuat di `packages/ui/src/components/data-table/` (`DataTableCard`, `DataTableHeader`, `DataTableRow`+`DataTableCell`, `DataTableEntityCell`, `StatusBadge` 5 varian). Migrasi `izin-table.tsx` **DITUNDA** — row-nya sudah punya keyboard-a11y custom (tabIndex/role/onKeyDown dari perbaikan audit UI sebelumnya) dan badge "Belum diisi" pakai `primary-tint`/`primary-hover` yang bukan salah satu dari 5 varian `StatusBadge`; migrasi paksa berisiko regresi a11y atau membuka varian warna ke-6 di luar scope. Evaluasi ulang kalau ada task lanjutan yang menyentuh file itu.
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T059-komponen-activity-feed-generik|T059 — Komponen Generik: Activity Feed]] *(tidak depend T057/T058, bisa paralel — belum ada pemakai nyata sampai TV Piket/Dashboard Kepsek mulai dikerjakan)* — dibuat di `packages/ui/src/components/activity-feed/` (`ActivityFeedCard` dengan `maxHeight` default 240-320px + scroll internal, `ActivityFeedItem` dengan 4 varian `iconChipColor`). Verifikasi: 10 item dummy, scrollHeight 520px vs clientHeight 240px — clip+scroll terkonfirmasi via Playwright.
- [ ] [[Projek/AbsenSI/06-Features/tasks/T060-audit-visual-dashboard-guru-admin-jurnal|T060 — Audit Visual Nyata: Dashboard Guru & Admin Jurnal (Playwright)]] ⚠️ *paling penting untuk sesi berikutnya — T038-T054/T056 belum pernah diverifikasi visual sama sekali*

## 🏗️ Blok 10 — Migrasi Database Lama (BARU, 2026-07-22, semuanya BELUM DIKERJAKAN)
> AbsenSI adalah rebuild dari aplikasi Laravel lama. Audit struktur (`Projek/AbsenSI/06-Features/migrasi-database-lama.md`) menemukan field biodata guru & wali murid siswa yang hilang saat rebuild — sudah diputuskan ditambahkan kembali. **Ini scope di LUAR "Fase 2 Jurnal" murni** (lebih ke perbaikan Fase 1 Core/Students/Teachers), tapi ditaruh di tracker yang sama karena ditemukan & diputuskan dalam rangkaian sesi yang sama. Urutan: T061+T063 (schema, bisa paralel) → T062 (ETL, WAJIB dry-run dulu, JANGAN langsung --execute).

- [x] (**kode ada, belum commit — ditemukan 2026-07-22 saat cek dependency T065**) [[Projek/AbsenSI/06-Features/tasks/T061-schema-tambahan-biodata-lama|T061 — Schema: Tambah Field Biodata Guru dari Database Lama]] — field `gelarDepan/gelarBelakang/tempatLahir/tanggalLahir/jenisKelamin/agama/alamat/statusPernikahan/statusKepegawaian` sudah ada di model `Teacher`, terverifikasi `DESCRIBE teachers` via MySQL MCP — migrasi SUDAH diterapkan ke DB live, bukan cuma di schema.prisma.
- [x] (**kode ada, belum commit — ditemukan 2026-07-22**) [[Projek/AbsenSI/06-Features/tasks/T063-schema-data-wali-murid|T063 — Schema: Tambah Data Wali Murid ke Student]] — field `namaAyah/namaIbu/rtRw` dkk sudah ada di model `Student` (terlihat di `core-types.ts` & schema.prisma), belum diverifikasi ulang senyakin T061 tapi pola sama.
- [ ] [[Projek/AbsenSI/06-Features/tasks/T062-etl-migrasi-data-lama|T062 — ETL Script: Migrasi Data dari Database Lama]] ⚠️ **BELUM ADA** (tidak ditemukan file ETL di `apps/api/src`) — *depend T061+T063 [selesai] — JANGAN --execute tanpa review --dry-run bersama user*

## 🏗️ Blok 11 — Form Input Modern: Sheet + Tabs (BARU, 2026-07-22 — T064/T065/T066 SEMUA dieksekusi 2026-07-22)
> Form `Tambah Guru`/`Tambah Siswa` existing pakai popup `Dialog` kecil sederhana — sudah 11 field (siswa) dan akan bertambah ~9-13 lagi dari Blok 10 (T061/T063). Pola baru "Sheet + Tabs" ditambahkan ke `design-system/03-components.md` (bagian "Form Input Panjang") sebagai **rujukan WAJIB untuk SEMUA form >6 field ke depan**, bukan cuma guru/siswa. **T064 harus selesai duluan** (primitif dasar), lalu T065/T066 depend ke T061/T063 (field baru harus sudah ada di backend sebelum form-nya dibuat).
>
> ✅ **Ditemukan saat T066, DIPERBAIKI saat verifikasi T067 (2026-07-23):** endpoint `POST /students`/`PATCH /students/:id` (`students.controller.ts`) TIDAK punya `@LogActivity`, melanggar aturan permanen di catatan #16 bawah. Gap PRA-EXISTING ini sudah ditutup (`student.create`/`student.update` ditambahkan) — lihat Blok 12.

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T064-primitif-sheet-tabs|T064 — Primitif UI Baru: Sheet & Tabs]] — dibuat di `packages/ui/src/components/ui/sheet.tsx` (berbasis `@radix-ui/react-dialog`, sama seperti `Dialog` existing) dan `tabs.tsx` (baru install `@radix-ui/react-tabs`). Verifikasi Playwright: lebar Sheet 560px (dalam rentang 480-560px spec), radius `24px` HANYA di sudut kiri (`borderTopLeftRadius`/`borderBottomLeftRadius`), sisi kanan `0px`; footer tetap terlihat (posisi tidak berubah) saat body di-scroll dengan 20 dummy field; Tabs aktif render solid `#F5841F`+teks putih, tidak aktif border tipis+teks gelap, semua `border-radius: 9999px` (pill), TIDAK ada underline indicator.
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T065-form-guru-sheet-lengkap|T065 — UI: Form Guru — Migrasi ke Sheet + Field Biodata Lengkap]] — `guru-view.tsx` migrasi `Dialog`→`Sheet`, 3 tab (Data Pokok/Biodata/Kontak & Alamat), semua field baru opsional kecuali NIY+Nama. Backend: `CreateTeacherDto` diperluas + `teachers.service.ts` diperbaiki (bug ditemukan saat testing: `data: dto` blind-spread gagal karena `tanggalLahir` string mentah bukan `Date`, sekarang eksplisit `new Date(dto.tanggalLahir)` sama seperti pola `students.service.ts`). Verifikasi: submit minimal (NIY+Nama saja) berhasil, submit lengkap semua field berhasil + dicek `SELECT * FROM teachers` via MySQL MCP cocok persis, data persist saat pindah tab, footer sticky. **Ada 2 baris data uji coba tertinggal di tabel `teachers`** (niy `199001012020011099` dan `199912312099011000`) — MySQL MCP read-only jadi tidak bisa dihapus dari sini, dan UI Guru belum ada tombol hapus; user perlu hapus manual.
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T066-form-siswa-sheet-lengkap|T066 — UI: Form Siswa — Migrasi ke Sheet + Field Wali Murid & Status Lulus]] — `siswa-view.tsx` migrasi `Dialog`→`Sheet`, 4 tab (Data Pokok/Biodata/Kontak & Alamat/Data Wali). Logic kondisional 2-tingkat (`Status=Nonaktif` → tampilkan Alasan Nonaktif → `Alasan=Lulus` → tampilkan Tahun Lulus) diverifikasi Playwright: semua transisi (nonaktif→lulus→tahun muncul, lulus→mengundurkan_diri→tahun hilang lagi, balik ke aktif→semua hilang) sesuai spec persis. Backend: `CreateStudentDto` diperluas 6 field baru (`status`, `alasanNonaktif`, `tahunLulus`, `noHpSiswa`, `noHpAyah`, `noHpIbu` — field T063 ternyata belum pernah di-wire ke DTO, sama seperti temuan T065 di Teacher) + `students.service.ts` create/update disesuaikan. Tidak ada form edit siswa terpisah (dicek di `siswa-detail-view.tsx`, cuma ada Dialog hapus-foto — benar tetap Dialog, ≤6 field). Verifikasi: submit minimal (NISN+Nama+Kelas) berhasil, submit lengkap semua field + `SELECT * FROM students` via MySQL MCP cocok persis, badge status pakai `StatusBadge` shared (ikon per varian, bukan hand-rolled). **2 baris data uji coba tertinggal** (nisn `2026999001`, `2026999002`) — sama seperti T065, tidak ada tombol hapus siswa di UI dan MySQL MCP read-only, perlu dihapus manual.

## 🏗️ Blok 12 — Kelengkapan Activity Log (BARU, 2026-07-22 — DIVERIFIKASI LENGKAP 2026-07-23)
> Audit menemukan **14 dari 22 controller** dengan endpoint mutasi TIDAK memakai `@LogActivity` — termasuk aksi berdampak luas (aktivasi semester, CRUD guru, izin guru+upload bukti, perubahan jadwal blok). Infrastrukturnya sudah ada (`ActivityLogInterceptor` + decorator), ini murni menambahkan decorator yang hilang secara konsisten.

- [x] (**diverifikasi lengkap 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T067-lengkapi-activity-log-semua-controller|T067 — Lengkapi @LogActivity di Semua Controller Mutasi yang Belum Tercatat]] — semua 14 controller dari tabel spec diaudit satu-per-satu: `kampus`/`kelas`/`jurusan`/`teachers`/`schedules`/`mapel` ditambah decorator; `schedule-config`/`semesters`/`block-week-ranges`/`teacher-permits`/`photos` ternyata sebagian besar sudah log manual via `ActivityLogService.record()` di service layer (tidak boleh ditambah decorator lagi, akan dobel-catat) — hanya endpoint yang benar-benar belum tercatat yang ditambahkan (`semester.create`, `teacher_permit.submit_tugas`, dll); `teaching-sessions` (`start` via decorator, `generate-today`/`run-auto-close` manual karena bulk/tanpa 1 entity target), `journal` (manual, karena lookup key beda dari `id` bawaan decorator), `import` (1 log ringkasan per operasi, bukan per-baris). **Gap tambahan ditemukan+diperbaiki di luar tabel asli 14 controller:** `students.controller.ts` (`POST /students`, `PATCH /students/:id`) TIDAK punya `@LogActivity` sama sekali — celah pra-existing yang sempat dicatat di Blok 11 tapi belum diperbaiki sampai sesi ini, sekarang sudah ditambahkan `student.create`/`student.update`. Verifikasi: `tsc`+`jest` (171/171)+`nest build` bersih, tidak ada `action` name bentrok di seluruh codebase, DAN diverifikasi live lewat API+MySQL (`kampus.create`, `academic_year.create`, `semester.create`, `semester.activate`, `teacher_permit.create`, `teacher_permit.update_bukti` semua tercatat dengan `snapshot_before`/`snapshot_after` benar).

## 🏗️ Blok 13 — TV Piket (BARU, 2026-07-22, spec DIFINALISASI hari ini — T068-T070 SEMUA dieksekusi 2026-07-23)
> `tv-piket.md` sebelumnya `planning-interview` dengan 5 open question ("menunggu sketsa") — **dituntaskan 2026-07-22** tanpa sketsa, berdasarkan referensi EzMart + komponen generik `DataTableCard`/`ActivityFeedCard` (T058/T059) yang sudah ada. Layout final: bar persentase 1 baris + 2 kolom tengah (siswa tidak hadir + guru belum mulai) + 1 baris bawah (guru izin). Auth pakai token TANPA expiry (beda dari TV Kepsek 30 hari) karena TV ini murni read-only — tapi WAJIB ada mekanisme revoke manual oleh `super_admin`. Urutan: T068 (auth/token) → T069 (API data, reuse Rekap Fase 1) → T070 (UI).

- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T068-schema-auth-tv-piket|T068 — Schema & Auth: Token TV Piket (Tanpa Expiry, Revocable)]] — model `TvSession` (migrasi `20260723065250_add_tv_sessions`), `TvPiketGuard` (adaptasi `KioskGuard`, TANPA IP whitelist — beda scope), `apps/api/src/tv-sessions/`. Verifikasi curl+MySQL: create/list/revoke, 401 token invalid/revoked, 403 role selain super_admin, token TIDAK PERNAH bocor di list ATAU revoke response (bug ditemukan+diperbaiki: `revoke()` awalnya return raw row termasuk token mentah, sekarang pakai `LIST_SELECT`), `@LogActivity` tercatat dengan `token` di-redact dari `snapshot_before`/`snapshot_after` (dicek langsung isi `activity_log`).
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T069-api-data-tv-piket|T069 — API: Data Agregat TV Piket (4 Widget)]] *(depend T068)* — `apps/api/src/tv-piket/`, reuse `AttendanceReportService.resolveHariWajib` (di-export dari `AttendanceModule`, sebelumnya tidak) dan pola `hitungTerlambatMenit` (T042). **Bug ditemukan+diperbaiki:** widget persentase awalnya filter `status: "aktif"` pada query siswa — ternyata `AttendanceReportService.report()` (Rekap Fase 1) TIDAK filter status sama sekali, jadi angka TV tidak cocok dengan Rekap Admin (melanggar acceptance criteria "1 sumber kebenaran"). Filter dihapus, sekarang angka persis sama — diverifikasi cross-check `GET /attendance/report` vs `GET /tv-piket/data` untuk kampus/tanggal yang sama. `guruIzin` di-scope kampus lewat `session.kelas.kampusId` (izin per-sesi) atau jadwal guru hari itu (izin seharian, `sessionId` null — `TeacherPermit` tidak punya kampusId langsung).
- [x] (**kode ada, belum commit**) [[Projek/AbsenSI/06-Features/tasks/T070-ui-halaman-tv-piket|T070 — UI: Halaman TV Piket]] *(depend T068 [selesai], T069 [selesai], DataTableCard/ActivityFeedCard T058/T059 [selesai])* — route `/tv-piket/[kampusId]`, hooks `use-tv-piket-data.ts` (fetch+Socket.IO+polling fallback), `use-auto-scroll.ts` (generik, reusable), `tv-piket-init.ts` (pola ADR-021, token dari `?token=` → localStorage), proxy `/api/tv-piket-proxy` (server-side inject Bearer, pola sama `apps/kiosk/api/tap`). **Bug ditemukan+diperbaiki:** halaman awalnya redirect ke `/login` — middleware Next.js men-cek cookie JWT di SEMUA route kecuali `/login`, padahal `/tv-piket` sama sekali tidak pakai auth user (token `tv_sessions` statis lewat header, bukan cookie) — ditambahkan ke `PUBLIC_PATHS` bareng `/api/tv-piket-proxy`. Bar persentase juga diperbaiki: segmen 0% awalnya tetap coba render 2 baris teks dan bertabrakan/wrap — sekarang teks disembunyikan kalau segmen <8%. Socket.IO gateway (`attendance.gateway.ts`) ditambah cabang auth `tvPiketToken` (auto-join `attendance:kampus:{id}`, tanpa perlu `join:kampus` manual dari client) — dikonfirmasi lewat log server "TV Piket terhubung". Auto-scroll diverifikasi dengan data sintetis 25 baris (network intercept Playwright): scroll turun (0→870), lalu terkonfirmasi sudah scroll balik naik (347) di titik berikutnya — siklus turun→jeda→naik→jeda→ulang bekerja. Reload tanpa `?token=` tetap jalan (localStorage). Token revoked → halaman error jelas, bukan blank/crash.

## 🏗️ Blok 14 — Plotting Siswa ke Kelas (BARU, 2026-07-22, semua BELUM DIKERJAKAN)
> Celah ditemukan: siswa cuma bisa di-assign ke kelas SEKALI saat form "Tambah Siswa", tidak ada mekanisme pindah kelas maupun penempatan massal. Spec lengkap: [[Projek/AbsenSI/06-Features/plotting-siswa-kelas|plotting-siswa-kelas.md]]. 3 mekanisme terpisah + 1 fitur terkait (Tandai Keluar): (1) Penempatan siswa baru via paste-NISN dengan validasi visual hijau/merah real-time, (2) Kenaikan kelas massal — **REVISI 2026-07-22: 1 halaman semua kelas sekaligus**, dropdown tujuan per baris, opsi "Lulus" khusus kelas XII, (3) Pindah kelas individual dari halaman detail siswa, (4) Tandai siswa Lulus/Mengundurkan Diri. **T075 (kolom tingkat) WAJIB sebelum T073** — dibutuhkan untuk logic "opsi Lulus cuma di kelas XII". Urutan: T071+T075 (schema, paralel) → T072 → T073 (revisi total, baca ulang task file-nya) → T074 independen kapan saja.

- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T071-schema-tinggal-kelas|T071 — Schema: Field Tinggal Kelas + Kolom Jumlah Siswa]] — kolom `tinggal_kelas_pada` (nullable `date`) ditambah ke `Student` (migrasi `20260723090134_t071_tinggal_kelas`), terpisah dari `alasanNonaktif`/`status` sesuai spec. `GET /kelas` diperluas dengan field `jumlahSiswa` (Prisma `_count` filtered relation, `where: { status: "aktif" }`). Diverifikasi live: kelas dengan 1 siswa aktif + 1 nonaktif → `jumlahSiswa: 1` (bukan 2), cocok manual `COUNT` via MySQL; kolom `tinggal_kelas_pada` terkonfirmasi ada di `DESCRIBE students`.
- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T075-schema-tingkat-kelas|T075 — Schema: Kolom Tingkat Terpisah di Kelas]] — enum `Tingkat` (X/XI/XII) + kolom `kelas.tingkat`, migrasi 2 tahap sesuai spec (`20260723122811_t075_tingkat_kelas_nullable` → backfill → `20260723123319_t075_tingkat_kelas_not_null`). Script `apps/api/scripts/backfill-tingkat-kelas.ts` (PERMANEN, jangan dihapus — untuk jaga-jaga kalau database perlu di-reset & backfill ulang) parse token pertama `nama` (split whitespace, exact-match — bukan substring, jadi tidak ada risiko "X" salah cocok ke "XI"/"XII"). Dry-run direview user dulu sebelum `--execute` sesuai keputusan eksplisit. Hasil: 3/3 kelas existing ter-backfill benar ("X RPL 1"→X, "X TKJ 1"→X, "XI TKJ 1"→XI), `nama` tidak berubah. `CreateKelasDto` wajib `tingkat` (enum-validated), `UpdateKelasDto` opsional (partial). Dropdown Tingkat ditambahkan ke form Kelas existing (`kelas-jurusan-view.tsx`) + kolom tabel baru. Diverifikasi live: POST tanpa `tingkat` → 400 dengan pesan jelas, POST dengan `tingkat` → 201 berhasil, `GET /kelas` mengembalikan `tingkat` untuk semua baris.
- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T072-plot-siswa-baru-ke-kelas|T072 — UI+API: Penempatan Siswa Baru ke Kelas (Paste-NISN)]] — `POST /students/validate-nisn-batch` (klasifikasi `valid`/`sudah_ada_kelas_lain`/`tidak_ditemukan`), `POST /kelas/:id/plot-siswa` (re-validasi server-side per-item, TIDAK percaya FE, `@LogActivity action: student.plot_kelas`). UI: kolom "Jumlah Siswa" + tombol "Plot Siswa" di tabel Kelas, halaman `/kelas/[id]/plot-siswa` 2 kolom (textarea paste-NISN debounce 500ms + preview `StatusBadge` hijau/merah per baris, tombol Input kirim HANYA baris valid & reset form tanpa reload, kolom kanan akumulatif sesi). **2 gap arsitektur ditemukan mid-task, diperbaiki dengan konfirmasi eksplisit user (bukan diasumsikan):** (1) `Student.kelasId` semula `NOT NULL` di schema+DB — kontradiksi langsung dengan premis fitur (siswa belum ditempatkan = `kelasId` null). Fix: migrasi `20260723125329_make_student_kelas_id_nullable`, 14 error TS18047 di `attendance-report.service.ts`/`attendance.service.ts`/`permits.service.ts`/`students.service.ts`/`tv-piket.service.ts` diperbaiki (assertion `!` di tempat `where` clause sudah jamin non-null via relation filter, optional-chaining `?.` di authorization check `.kelas?.kampusId !== kampusId` — deny-by-default utk siswa tanpa kelas). (2) Setelah fix #1, ternyata TIDAK ADA jalur produksi yang menghasilkan siswa `kelasId: null` — `CreateStudentDto.kelasId` masih wajib (`@IsInt`), `import.service.ts` selalu wajib match kelas+jurusan. Fix: `kelasId` opsional di `CreateStudentDto`+`StudentsService.create()`, kolom `kelas`/`jurusan` opsional di CSV import (kosong berdua = siswa baru tanpa kelas; kalau salah satu diisi wajib keduanya match kelas existing). Verifikasi: `tsc`+`jest`(171/171) bersih di kedua titik perbaikan, `pnpm turbo run build` bersih 6/6 (termasuk route baru), live end-to-end via curl+MySQL — 3 NISN campuran → badge status benar, plot sukses → `kelas_id` ter-update, retry sama → ditolak race-condition dengan alasan jelas, `activity_log` tercatat utk sukses MAUPUN gagal, data test dibersihkan pasca-verifikasi.
- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T073-kenaikan-kelas-massal|T073 — UI+API: Kenaikan Kelas Massal (Menu Tersendiri, Semua Kelas Sekaligus)]] — `POST /kelas/kenaikan-massal`, 1 transaction untuk semua kelas (all-or-nothing), validasi backend `lulus:true` hanya kelas `tingkat: XII` (400 kalau tidak), `nisnTinggalKelas` per-item exclude siswa + set `tinggalKelasPada`, `@LogActivity` manual (`action: kelas.kenaikan_massal`, tidak ada `:id` tunggal untuk decorator otomatis). UI `/kelas/kenaikan-massal`: dropdown Tahun Ajaran Tujuan + input Tahun Lulus eksplisit (bukan auto-parse — keputusan user, format `AcademicYear.nama` tidak reliable diparse), tabel semua kelas dengan dropdown tujuan per baris (opsi "Lulus" cuma muncul di kelas XII) + popover "Atur" NISN Tinggal Kelas (reuse validate-nisn-batch T072, label direinterpretasi), dialog konfirmasi ringkasan. Diverifikasi live end-to-end via curl+MySQL: batch campuran (naik normal + lulus + tinggal kelas) semua state benar, validasi XII-only ditolak 400, rollback dikonfirmasi via code (`prisma.$transaction`), `activity_log` tercatat lengkap. Detail lengkap di tabel status atas.
- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T074-pindah-kelas-individual-dan-tandai-keluar|T074 — UI: Pindah Kelas Individual + Tandai Siswa Keluar]] — UI-only di `siswa-detail-view.tsx`, TIDAK ada endpoint baru (pakai `PATCH /students/:id` existing). Field Kelas jadi editable (dropdown semua kelas), tombol "Tandai Keluar" (Dialog kecil, bukan Sheet — sesuai `03-components.md` ≤6 field) dengan Alasan (Lulus/Mengundurkan Diri/Lainnya) + Tahun Lulus kondisional (hanya alasan="lulus"), tombol "Aktifkan Kembali" untuk reversibility. Diverifikasi live via curl+MySQL: pindah kelas, tandai keluar lulus+tahunLulus, aktifkan kembali (field lama stale tapi harmless — pola sama T066), tandai keluar mengundurkan diri tanpa tahunLulus. `tsc` bersih, `pnpm turbo run build` bersih 6/6. Detail lengkap di tabel status atas. **Blok 14 SEKARANG LENGKAP 5/5.**

## 🏗️ Blok 15 — Direktori Siswa Dashboard Piket (LENGKAP 2026-07-23)
> Celah ditemukan: dashboard piket cuma punya daftar kontekstual (tap hari ini, belum kembali, terkunci), tidak ada direktori siswa yang bisa dicari/difilter. Spec: [[Projek/AbsenSI/06-Features/dashboard-piket|dashboard-piket.md]] bagian "6b" (baru). **REUSE halaman detail siswa existing** (foto+biodata+riwayat catatan sudah lengkap di `/admin/siswa/[id]`) — task ini murni buka akses role + 1 halaman list baru, BUKAN membangun halaman detail dari nol. **Pengecualian sadar**: scope LINTAS KAMPUS (beda dari semua fitur piket lain yang di-scope kampus).

- [x] (**dieksekusi+diverifikasi 2026-07-23**) [[Projek/AbsenSI/06-Features/tasks/T076-direktori-siswa-dashboard-piket|T076 — UI+API: Direktori Siswa (Search & Filter) di Dashboard Piket]] — `guru_piket` dibuka HANYA `GET` di 4 controller (students, attendance/riwayat-catatan, kelas, jurusan), mutasi tetap eksklusif admin. Filter `nama` (search) baru ditambah ke `ListStudentsDto`. Scope LINTAS KAMPUS sengaja (ADR-015 exception), dicatat eksplisit di kode. **Blocker arsitektur ditemukan+diperbaiki (user confirm):** `(admin)/layout.tsx` redirect UNCONDITIONAL guru_piket sebelum child render, `/siswa/[id]` sama sekali tidak terjangkau — ditambah mekanisme header `x-pathname` dari middleware supaya layout Server Component bisa baca pathname, exception HANYA untuk regex `/siswa/{angka}` (bukan list, bukan route admin lain). Shell Sidebar/AdminContent sengaja TIDAK diganti (keputusan user, scope minimal). `siswa-detail-view.tsx` dapat prop `readOnly` (dihitung server-side dari role) — sembunyikan semua tombol mutasi. Halaman baru `/piket/siswa` (search debounced + filter Kelas/Jurusan, klik baris → detail). Diverifikasi live via curl dengan akun `guru_piket` sungguhan: SEMUA 7 acceptance criteria di spec terverifikasi (cross-kampus search jalan, GET terbuka, POST/PATCH tetap 403). `tsc`+`jest`(171/171) bersih, `pnpm turbo run build` bersih 6/6. Detail lengkap di tabel status atas. **Blok 15 SEKARANG LENGKAP — FASE 2 TUNTAS kecuali T055/T060/T062.**

## 🏗️ Blok 16 — Polish Admin: Guru/Kartu/Foto (BARU, 2026-07-24)
> Feedback langsung dari uji coba user di halaman admin 2026-07-24: menu Guru belum ada CRUD (Edit/Nonaktifkan) dari UI meski API-nya sudah ada; menu Kartu & Foto Tersimpan menampilkan siswa+guru campur dalam 1 tabel, perlu dipisah section + filter Kelas/Jurusan + kolom Jurusan (siswa saja, guru tidak punya jurusan di skema). Semua UI-only, tidak ada perubahan skema Prisma.

- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T077-crud-guru|T077 — UI: Lengkapi CRUD Guru (Edit, Detail, Nonaktifkan)]] — tambah kolom Aksi (`w-16`, pola tombol icon identik `kartu-view.tsx`) dengan tombol `Pencil` per baris di tabel Guru (`guru-view.tsx`). `GuruForm` di-refactor jadi dual-mode (`mode: "create" | "edit"`, prop `initialData?: Teacher`) — SATU komponen, bukan duplikasi, semua field pre-fill dari `initialData` saat edit (termasuk `tanggalLahir` string→`Date` untuk `DatePicker`). Submit: `mode === "edit"` → `PATCH /teachers/:id` (endpoint sudah ada sebelumnya, tidak ada perubahan backend), selain itu tetap `POST /teachers` seperti semula. Field NIY tetap bisa diedit + warning caption di bawahnya saat mode edit ("Mengubah NIY tidak memengaruhi riwayat presensi yang sudah tercatat"), sesuai spec. Status "Nonaktifkan" TIDAK dapat tombol/dialog terpisah — cukup lewat dropdown Status yang sudah ada di form yang sama (`StatusBadge` variant `neutral` untuk nonaktif sudah ada dari sebelumnya, tidak berubah). TIDAK ada endpoint atau tombol DELETE, TIDAK ada halaman detail terpisah `/guru/[id]` — sesuai 2 larangan eksplisit di spec. Diverifikasi live via curl (payload sama persis bentuk yang dikirim form edit): buat 1 guru test, `PATCH` ubah nama+status ke `nonaktif` → response benar, dikonfirmasi ulang via MySQL MCP `SELECT` langsung ke tabel `teachers`. `tsc` bersih, full `pnpm turbo run build` bersih 6/6 (`/guru` route 4.7kB→5.26kB mencerminkan UI baru). Data test dibersihkan pasca-verifikasi.
- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T078-kartu-split-section-filter|T078 — UI: Split Section Kartu (Guru/Siswa) + Filter Kelas & Jurusan + Kolom Jurusan]] — `kartu-view.tsx` di-refactor jadi `Tabs` 2 section ("Kartu Siswa" default, "Kartu Guru") di halaman `/kartu` (1 route, bukan 2 halaman terpisah). Tab Siswa dapat kolom Jurusan+Kelas dan filter dropdown Jurusan+Kelas yang saling mempersempit (pilih Jurusan otomatis reset Kelas kalau tidak lagi cocok, opsi Kelas difilter client-side by `jurusanId`). Tab Guru TIDAK dapat kolom/filter Jurusan sesuai larangan spec. Backend: `cards.service.ts` — `include` Prisma di 3 lokasi (`findAll`, `create`, `replace`) diperluas `student.kelas.jurusan` (sebelumnya cuma `id/nama/nisn`, tidak ada kelas). Tombol Registrasi Kartu/Import Data/Tap-to-Assign dipindah ke level shared (tampil di kedua tab, bukan diduplikasi). `page.tsx` fetch tambahan `kelasList`+`jurusanList` (endpoint `/kelas`, `/jurusan` yang sudah ada). Diverifikasi: `tsc` bersih, `next build` bersih (route `/kartu` compile, 5.46kB), live `GET /cards` via curl mengembalikan `student.kelas.jurusan` lengkap setelah rebuild+restart API (dev server sempat jalan mode `pnpm start`/production build, bukan `--watch`, jadi perlu rebuild manual `nest build` + restart proses). Verifikasi visual Playwright TIDAK dilakukan — browser MCP sedang dipakai sesi lain bersamaan, tidak dipaksa ambil alih.
- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T079-foto-split-section-jurusan|T079 — UI: Split Section "Foto Tersimpan" (Guru/Siswa) + Kolom Jurusan]] — `foto-view.tsx`: array gabungan `hasFotoEntries` dipecah jadi `fotoGuru`+`fotoSiswa` (masing-masing sort by nama), dirender 2 sub-tabel terpisah ("Foto Guru"/"Foto Siswa") di dalam 1 card yang sama (bukan `Tabs` terpisah — sesuai spec, section ini lebih ringan dari T078). Kolom "Tipe" yang lama DIHAPUS dari kedua tabel (implisit dari section-nya). Section Foto Siswa dapat 2 kolom baru: Jurusan + Kelas (dari `student.kelas.nama`/`student.kelas.jurusan.nama` — sudah ada di response `GET /students` tanpa perlu ubah backend, dikonfirmasi live lewat 156+ data foto guru & banyak data foto siswa produksi nyata). Section Foto Guru TIDAK dapat kolom itu (guru tidak punya jurusan di skema), sesuai larangan eksplisit spec. Logic hapus foto (`DeletePhotoForm`) TIDAK diduplikasi — komponen baru `FotoRow` (shared row renderer, prop `showJurusanKelas` opsional) dipakai kedua tabel, keduanya tetap panggil `onDelete` yang sama ke 1 `DeletePhotoForm`/`Dialog` di level parent. Flow upload/assign di bagian atas halaman TIDAK disentuh sama sekali, sesuai larangan scope. `tsc` bersih, full `pnpm turbo run build` bersih 6/6 (`/foto` route 3.57kB→3.81kB). Tidak ada data test dibuat/dihapus — verifikasi pakai data produksi nyata yang sudah ada (read-only check via curl, tidak ada mutasi).

## 🏗️ Blok 17 — Pagination, PKL, Akun Piket, Kalender 12-Bulan, Rename Mesin (BARU, 2026-07-24)
> Feedback lanjutan dari uji coba user 2026-07-24 setelah data migrasi (4090 siswa, 3929 kartu) membuat performa render tabel terasa berat, plus 5 permintaan fitur/UX terpisah. Prioritaskan T080 (pagination) lebih dulu kalau volume data terus jadi keluhan performa — sisanya independen, bisa dikerjakan urutan bebas.

- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T080-pagination-tabel-besar|T080 — API+UI: Pagination untuk Tabel Data Besar (Siswa, Kartu, Foto, Log)]] — **Keputusan cakupan dikonfirmasi user**: awalnya diminta sebagai "eksekusi T060" dengan deskripsi dropdown page-size kelipatan 40 sampai 240 lalu 1000 — dicek dulu, T060 ternyata task AUDIT VISUAL murni (tanpa kode), jadi dikonfirmasi ulang ke user dan ternyata yang dimaksud T080 (Pagination) dengan dropdown custom, BUKAN Previous/Next polos seperti draf awal spec. **Backend**: `page`/`pageSize` OPSIONAL ditambah ke `ListStudentsDto`+`students.service.ts` dan `ListCardsDto`+`cards.service.ts` — tanpa `page` tetap return array polos (backward compatible, TIDAK breaking `plot-siswa`/`kenaikan-massal`/`assign-picker` foto yang butuh SEMUA data), dengan `page` return `{ data, total, page, pageSize }` (pola sama `ActivityLogPage`/`log-view.tsx`). `ListCardsDto` juga dapat filter baru `ownerType`(student/teacher)+`kelasId`+`jurusanId` supaya tab Kartu Siswa/Kartu Guru (T078) bisa di-paginate server-side per tab. `ListStudentsDto` dapat filter baru `hasFoto` (boolean, custom `@Transform` karena query string `"true"` tidak otomatis ke-coerce `@Type(() => Boolean)`) supaya pagination halaman Foto Siswa cocok persis dengan baris "sudah punya foto" yang ditampilkan (bukan paginate SEMUA siswa lalu filter sisi klien, yang bikin 1 halaman bisa kosong). **Komponen shared baru**: `Pagination` di `packages/ui` (`data-table/pagination.tsx`) — dropdown "Tampilkan" kelipatan 40 sampai 240 lalu lompat ke 1000 (`PAGE_SIZE_OPTIONS` konstanta, keputusan user, BUKAN preseden `log-view.tsx` yang Previous/Next polos) + info "X data — halaman Y dari Z" + tombol Sebelumnya/Berikutnya, dipakai di 3 halaman. **Frontend `/siswa`**: infrastruktur `searchParams`+`router.push`+`router.refresh()` sudah ada (dipakai reuse), tambah `page`/`pageSize` ke filter, ganti filter (Kelas/Jurusan/Status) OTOMATIS reset ke halaman 1. **Frontend `/kartu`**: KONVERSI TOTAL dari client-side filter murni (semua filter di array sudah di-fetch penuh, TIDAK ada `searchParams` sama sekali) jadi server-side searchParams sepenuhnya — 2 hasil paginasi terpisah (`cardsSiswa`/`cardsGuru`, masing-masing `page`/`pageSize` sendiri: `siswaPage`/`guruPage` dst.) karena struktur Tabs 2 section T078. Handler mutasi (register/replace/revoke) disederhanakan jadi `router.refresh()` (bukan optimistic local state update) karena data sekarang datang dari Server Component, bukan `useState` yang di-derive sekali dari initial fetch. **Frontend `/foto`**: section Foto Siswa (T079) di-paginate server-side (`hasFoto=true` di query), section Foto Guru TIDAK di-paginate (jumlah jauh lebih kecil, tidak masalah performa) — `AssignPicker` (dropdown assign foto tak-tercocokkan) SENGAJA tetap pakai fetch terpisah `allStudents` TANPA `page` (sesuai larangan eksplisit spec "TIDAK perlu pagination untuk assign-picker dropdown"), jadi 1 halaman fetch `GET /students` 2x (paginated utk tabel, unpaginated utk dropdown). Diverifikasi live via curl+MySQL pada data produksi nyata: `GET /students` tanpa page → array 4090 (cocok persis angka di spec), dengan `page=1&pageSize=40` → `{data:40 items, total:4090}`, `pageSize=1000` → 1000 item; `GET /cards` tanpa page → array 3929 (cocok spec), `ownerType=student` → total 3646 semua `studentId` terisi, `ownerType=teacher` → total 283 semua `teacherId` terisi; `kelasId=1` → total 0 dikonfirmasi BENAR via MySQL langsung (kelas test lama tanpa kartu, bukan bug); `hasFoto=true` → total 2801 semua baris ada `foto`; `pageSize=1500` (>1000) → 400 ditolak validasi. `tsc` bersih kedua app (2x sempat terblokir sementara oleh pekerjaan paralel T078 lalu T081 yang belum selesai — dikonfirmasi via `git diff` bukan dari perubahan T080 sebelum lapor, ditunggu sampai masing-masing selesai baru lanjut, BUKAN dikerjakan/diperbaiki sendiri), `jest` 175/175 (angka sudah termasuk 4 test baru T081), full `pnpm turbo run build` bersih 6/6 (`/kartu` 4.9kB→5.57kB, `/siswa` 5.44kB→5.58kB, `/foto` 3.81kB→lebih besar — semua mencerminkan UI pagination baru). Tidak ada data test dibuat/dihapus — semua verifikasi read-only terhadap data produksi nyata yang sudah ada.
> **Gap ditemukan+diperbaiki 2026-07-25 (user report):** halaman `/piket/siswa` (Direktori Siswa, T076) TERLEWAT dari cakupan T080 — halaman ini fetch `GET /students` client-side murni (debounced search+filter di state lokal, BUKAN searchParams seperti `/siswa` admin), jadi waktu endpoint backend diberi pagination, halaman ini tetap manggil tanpa `page` dan dapat balikan array SEMUA 4091 siswa lintas kampus tiap kali user ketik di search box. Fix: `direktori-siswa-view.tsx` tambah state lokal `page`/`pageSize` (bukan URL — konsisten sama pola search/filter yang sudah local-state di halaman ini), reset ke halaman 1 tiap ganti nama/kelas/jurusan, render `Pagination` (component shared yang sama) di bawah tabel. Diverifikasi live sebagai akun `guru_piket` sungguhan (`piket1`, password di-reset ulang): `GET /students?page=1&pageSize=40` → `{data:40, total:4091}` benar, kombinasi `nama=a&page=1&pageSize=40` → filter+pagination jalan bareng. `tsc` bersih, `jest` 175/175, full build bersih 6/6 (`/piket/siswa` 2.06kB→2.17kB).
- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T081-menu-pkl-siswa|T081 — Schema+API+UI: Menu Khusus PKL Siswa Kelas XII]] — model baru `StudentPkl` (migrasi `20260724161455_t081_student_pkl`, pola sama `alasanNonaktif` — TIDAK mengubah `PersonStatus`/`kelasId`, insert-only-ish: baris baru tiap mulai, UPDATE `endedAt`/`tanggalSelesai` saat normalkan, histori tetap tersimpan). 5 endpoint baru di `students.controller.ts`/`service.ts`: `POST pkl/mulai`, `POST pkl/mulai-semua-xii`, `POST pkl/normalkan`, `POST pkl/normalkan-semua-xii`, `GET pkl` (list aktif) — semua static routes ditaruh SEBELUM `:id` param route (konsisten pola `validate-nisn-batch`). Validasi NISN PKL jadi VARIAN TERPISAH (`validateNisnBatchPkl`, endpoint `pkl/validate-nisn-batch`) dari punya T072 — semantik "valid" beda (wajib sudah kelas XII, bukan `kelasId` null). `@LogActivity` manual (bukan decorator — bulk, tanpa 1 entity target) dengan 4 action name terpisah (`pkl_mulai`/`pkl_mulai_massal`/`pkl_normalkan`/`pkl_normalkan_massal`), diverifikasi tercatat di `activity_log` dengan snapshot benar. `attendance-report.service.ts`: PKL menggantikan alfa di `report()` DAN `riwayatCatatan()` (overlap `tanggalMulai <= dateKey <= (tanggalSelesai ?? to/hari-ini)`), field baru `pkl: number` di `ReportRow`, `jenis: "pkl"` baru di `RiwayatCatatanEntry` — dihitung on-the-fly, BUKAN insert `AttendanceRecord`. 4 unit test baru khusus PKL (tanpa PKL tetap alfa normal; overlap penuh → semua pkl; PKL selesai di tengah rentang → split alfa/pkl; hari yang sudah hadir/izin tidak ikut dihitung pkl meski overlap) — total 175/175 test backend lulus. UI: halaman baru `/siswa/pkl` (paste-NISN kiri, aksi massal+dialog konfirmasi kanan, tabel PKL aktif dengan tombol Normalkan per-baris di bawah — 3 bagian sesuai spec), menu sidebar "PKL Siswa" (`nav-items.ts`, icon `Briefcase`). Badge "PKL" (StatusBadge variant `processing`) ditambah TERPISAH dari badge status aktif/nonaktif di `siswa-view.tsx` (tabel) dan `siswa-detail-view.tsx` (header detail + riwayat catatan icon/label/warna) — TIDAK menimpa badge existing. Kolom "PKL" baru ditambah di tabel Rekap Kehadiran (`rekap-view.tsx`), sejajar kolom Alfa. Istilah tampilan "PKL" (bukan "KL") dipakai konsisten di semua UI. Diverifikasi live end-to-end via curl+MySQL pada data produksi nyata: `pkl/mulai` 1 siswa XII sungguhan (id 56, "AURA DWI APRILIA") → `GET pkl` menampilkannya; rekap kelas yang sama hari itu → `alfa: 0, pkl: 1` persis; `pkl/normalkan` → list PKL kosong lagi, `kelas_id` di DB TETAP `53` (tidak berubah); validasi NISN PKL: siswa XII asli → `valid`, siswa kelas X → `bukan_kelas_xii` dengan nama kelas, NISN tidak dikenal → `tidak_ditemukan`. **Tidak dijalankan `mulai-semua-xii`/`normalkan-semua-xii` langsung ke seluruh populasi XII produksi** (terlalu invasif untuk verifikasi) — endpoint `mulai`/`normalkan` (single-siswa, helper internal yang sama) sudah cukup membuktikan logic intinya benar. `tsc` bersih kedua app, `next build` bersih 41/41 route (`/siswa/pkl` 2.78kB), `nest build` bersih.
- [x] (**dieksekusi+diverifikasi 2026-07-25**) [[Projek/AbsenSI/06-Features/tasks/T082-akun-piket-pilih-guru|T082 — API+UI: Field Pilih Guru saat Membuat Akun Piket]] — **Backend**: `UsersService.ensureLinksValid()` (dipanggil `create()` dan `update()`) ditambah 1 pengecekan baru — `role === guru_piket && !teacherId` → 400 "Role guru_piket wajib ditautkan ke teacherId", persis pola pengecekan `role === guru` yang sudah ada. Dicek dulu: TIDAK ada unique constraint `teacherId` di skema `User` (dikonfirmasi via grep schema.prisma) — jadi TIDAK perlu ubah apapun soal itu, sesuai larangan eksplisit spec ("JANGAN buat constraint 1-guru-1-akun"). **Frontend**: `akun-view.tsx` — kondisi render dropdown Guru diperluas dari `role === "guru"` jadi `role === "guru" || role === "guru_piket"` (dropdown Kampus yang sudah ada untuk `guru_piket` TIDAK disentuh, tetap ada — field Guru ini tambahan bukan pengganti). Tombol submit disabled kalau `(guru atau guru_piket) tanpa teacherId` ATAU `guru_piket tanpa kampusId`. Diverifikasi live via curl+MySQL 5 skenario: (1) `guru_piket` cuma `kampusId` tanpa `teacherId` → 400, (2) cuma `teacherId` tanpa `kampusId` → 400 (aturan lama tetap jalan), (3) keduanya terisi → 201, (4) `teacherId` yang sama dipakai akun `role: guru` DAN `role: guru_piket` sekaligus → 201 keduanya, dikonfirmasi via MySQL `teacher_id` sama di 2 baris `users` berbeda (no unique constraint benar-benar berfungsi), (5) `PATCH` akun `guru_piket` existing untuk hapus `teacherId` (set null) → 400 ditolak (re-validasi jalan juga di edit mode, bukan cuma create). **Insiden operasional ditemukan+diperbaiki saat verifikasi (di luar scope asal, dicatat untuk sesi mendatang):** port 3001 API berulang kali "menempel" ke proses `node dist/main` basi (sisa dari `nest build`/testing manual sebelumnya, BUKAN proses `nest start --watch` yang di-manage biasa) yang menyajikan kode LAMA — bikin verifikasi awal salah nunjukin fix "tidak berfungsi" padahal kodenya sudah benar. Root cause: `nest build` di `pnpm turbo run build` kemungkinan turut menjalankan/meninggalkan instance `dist/main` yang tidak ter-track proses biasa. Selalu cek `ss -ltnp | grep 3001` (bukan cuma cek proses `nest.js start`) kalau endpoint sepertinya "tidak merespons perubahan kode" — beberapa kali proses zombie ini yang jadi penyebab, bukan bug di kode. `tsc` bersih kedua app, `jest` 175/175, full `pnpm turbo run build` bersih 6/6 (`/akun` 3.44kB, tidak banyak berubah karena scope UI kecil). Data test dibersihkan pasca-verifikasi. **Blok 17 SEKARANG LENGKAP 5/5 (T080-T084 semua selesai).**
- [x] (**dieksekusi+diverifikasi 2026-07-25**) [[Projek/AbsenSI/06-Features/tasks/T083-kalender-tampilan-12-bulan|T083 — UI: Kalender Tampilan 12 Bulan Sekaligus (Grid 2x6)]] — `kalender-view.tsx` di-refactor: cursor state jadi `year` (bukan bulan), grid render loop 12 `MiniMonth` (komponen baru) dalam `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6` (2x6 penuh di layar ≥1280px, degradasi bertahap di layar sempit — bukan hardcode 6 kolom mutlak). Navigasi ganti `goToMonth`→`goToYear` (geser ±1 tahun), tombol "Hari Ini" reset ke tahun berjalan + scroll-into-view ke bulan berjalan (`monthRefs`). Header hari mini pakai konstanta baru `HARI_SATU_HURUF` (1 huruf, `calendar-utils.ts` — TIDAK mengubah `HARI_PENDEK` existing, cuma tambah). Hari libur tetap ditandai via `HOLIDAY_COLOR[holiday.jenis]` sebagai background sel kecil (bukan badge teks, sesuai spec — tidak muat di ukuran mini), title-tooltip native browser untuk keterangan lengkap. Klik tanggal manapun di bulan manapun tetap panggil `handleDayClick`→`onDayClick` yang SAMA PERSIS (logic dialog tambah/edit tidak disentuh). `buildMonthGrid`/`findHolidayForDate`/`isSameDay`/`toDateKey` di `calendar-utils.ts` TIDAK diubah sama sekali, hanya di-reuse — sesuai larangan eksplisit spec. Verifikasi: `tsc` bersih, `next build` bersih 41/41 route (`/kalender` 4.22kB→4.55kB mencerminkan grid 12-bulan). Verifikasi visual Playwright TIDAK dilakukan — browser MCP sedang dipakai sesi lain bersamaan (pola sama seperti keterbatasan verifikasi T078), diverifikasi via code review manual terhadap tiap acceptance criteria sebagai gantinya. Toggle "kembali ke tampilan 1-bulan" (opsional di spec) TIDAK ditambahkan — grid responsive 2x2→3x3→4x3→2x6 dinilai cukup tanpa perlu toggle terpisah, bisa direvisit kalau user melaporkan UX buruk di layar sempit nyata.
- [x] (**dieksekusi+diverifikasi 2026-07-24**) [[Projek/AbsenSI/06-Features/tasks/T084-rename-kiosk-ke-mesin|T084 — UI: Ganti Istilah "Kiosk" Menjadi "Mesin" di Seluruh Tampilan]] — semua teks JSX yang tampil ke user diganti "Kiosk"→"Mesin" di `kiosk-view.tsx` (page title, heading "Daftar Mesin", tombol "Tambah Mesin", label form "Nama Mesin"/"Tipe Mesin", dialog "URL Mesin — {nama}"/"Nonaktifkan Mesin", pesan error "Gagal menyimpan mesin", teks konfirmasi nonaktifkan), label sidebar admin (`nav-items.ts`: "Kiosk"→"Mesin"). **Ditemukan+diperbaiki juga di `apps/kiosk`** (device fisik gerbang, per larangan eksplisit spec "JANGAN lupa cek"): `layout.tsx` metadata `title`/`description` (tampil di tab browser), `not-configured-screen.tsx` (teks besar "Mesin belum dikonfigurasi" + "URL mesin yang benar" — layar yang muncul kalau device belum di-setup, langsung dilihat petugas gerbang). TIDAK rename type `Kiosk`/`KioskTipe`, model Prisma, tabel `kiosks`, route `/kiosk`, nama file, atau env var — semua dikonfirmasi tetap sesuai larangan eksplisit spec. Verifikasi persis pakai command dari spec sendiri: `grep -rn "Kiosk" apps/web/src --include="*.tsx"` — hasil akhir HANYA import type + nama fungsi/komponen/variable teknis, nol string JSX yang di-render. **Catatan proses:** typecheck sempat terblokir sementara oleh pekerjaan T078 (Kartu split) yang berjalan paralel di working tree yang sama (`kartu/page.tsx` sudah fetch `kelasList`/`jurusanList` tapi `kartu-view.tsx` belum menerima prop itu) — bukan disebabkan perubahan T084, dikonfirmasi via `git diff` sebelum lapor ke user; ditunggu sampai T078 selesai baru lanjut verifikasi. Setelah T078 selesai: `tsc` bersih di kedua app, `jest`(171/171) bersih, full `pnpm turbo run build` bersih 6/6 (`/kartu` 5.46kB mencerminkan T078, `/kiosk` size TIDAK berubah karena T084 murni ganti label bukan struktur).

## 🏗️ Blok 18 — Perbaikan Mesin/Kiosk: Deteksi IP, CRUD (BARU, 2026-07-26)
> Insiden nyata 2026-07-26: admin daftarkan mesin "test_laptop" dengan `allowedIp` dibaca dari `ipconfig` laptop itu sendiri (`10.200.219.194`), tap SELALU ditolak "IP tidak terdaftar" — log API membuktikan request sebenarnya sampai dari `192.168.123.2` (jaringan WiFi sekolah melakukan NAT/routing lintas-subnet, `TTL=62` di ping mengonfirmasi minimal 2 hop router di antaranya). Whitelist IP statis (ADR-021) rapuh terhadap topologi jaringan kompleks — 2 perbaikan: (1) bantu admin dapat IP yang AKURAT (dideteksi server, bukan ditebak dari client), (2) tutup gap CRUD supaya kesalahan input bisa diperbaiki langsung tanpa nonaktifkan+buat baru.

- [x] (**dieksekusi+diverifikasi 2026-07-26**) [[Projek/AbsenSI/06-Features/tasks/T085-deteksi-ip-kiosk-otomatis|T085 — API+UI: Deteksi IP Kiosk Otomatis Sebelum Registrasi]] — **Backend**: logic ekstraksi IP di `KioskGuard.extractClientIp()` (private method) DIPINDAH jadi fungsi shared `extractClientIp()` di `apps/api/src/common/client-ip.ts` (dipakai KioskGuard DAN endpoint baru — SATU sumber, bukan duplikasi, supaya IP yang "dideteksi" saat registrasi PASTI identik dengan yang "divalidasi" saat tap sungguhan). Endpoint baru `GET /kiosks/detect-ip` — TANPA guard sama sekali (device belum terdaftar saat dipanggil, chicken-and-egg kalau dipaksa token), response HANYA `{ detectedIp: string }`, tidak ada data sensitif lain. **Frontend**: `kiosk-view.tsx` form Tambah Mesin — section instruksi "Deteksi IP Otomatis" ditambah sebelum field IP Address, jelaskan kenapa `ipconfig` bisa beda dari IP yang dilihat server (NAT jaringan sekolah), kasih 2 link (halaman bantu `apps/kiosk` yang tampilan besar, atau endpoint API langsung) — admin buka dari BROWSER DI DEVICE KIOSK itu sendiri, salin manual ke field. Field `allowedIp` TETAP text input manual (TIDAK auto-fill lintas device, sesuai larangan eksplisit spec). **`apps/kiosk`**: halaman baru `/deteksi-ip` (tampilan IP besar, mudah dibaca petugas di depan device fisik) + proxy route `/api/detect-ip` (pola sama `kiosk-info/route.ts`, teruskan `X-Forwarded-For` dari `getClientIp()` supaya NestJS lihat IP device asli bukan IP loopback proxy). Diverifikasi live via curl: `GET /kiosks/detect-ip` TANPA auth header apapun → 200 `{detectedIp}` (dikonfirmasi bukan 401/403); dengan `X-Forwarded-For` di-spoof → echo persis nilai itu (match logic KioskGuard 1:1); **regresi dicek pada kiosk PRODUKSI NYATA dari insiden asli** (`test_laptop`, id 5, `allowedIp: 10.200.219.194`, `isActive: true`) — tap dengan `X-Forwarded-For` sama persis `allowedIp` → lolos guard (gagal di validasi DTO lain, BUKAN di pengecekan IP, membuktikan guard tetap lewat untuk IP yang benar), tap dengan IP lain → tetap 403 "IP tidak terdaftar" seperti semula, NOL regresi ke `KioskGuard`. **Insiden operasional berulang ditemukan+diperbaiki saat verifikasi (pola sama seperti T082, dicatat lagi karena masih terjadi):** 2x proses `node dist/main` basi (sisa build sebelumnya) menempel di port 3001 menyajikan kode lama, bikin endpoint baru sempat kelihatan 401 padahal kodenya sudah benar — diselesaikan dengan `ss -ltnp | grep 3001` untuk identifikasi PID pasti (bukan asumsi dari nama proses `nest.js start`), kill, lalu restart bersih. `tsc` bersih ketiga app (api/web/kiosk), `jest` 175/175, full `pnpm turbo run build` bersih 6/6 (`/kiosk` 9.97kB→10.3kB, kiosk app route baru `/deteksi-ip` 1.45kB + `/api/detect-ip`).
- [x] (**dieksekusi+diverifikasi 2026-07-26**) [[Projek/AbsenSI/06-Features/tasks/T086-crud-kiosk-mesin|T086 — UI+Fix: Lengkapi CRUD Mesin (Edit) + Tambah @LogActivity yang Hilang]] — **Backend**: `@LogActivity({action: "kiosk.update", targetType: "kiosk", prismaModel: "kiosk", sensitiveFields: ["deviceToken"]})` ditambah ke `kiosks.controller.ts` method `update()` (persis pola `create`/`deactivate`/`rotateToken` di controller yang sama) — fix gap pelanggaran aturan permanen T067. **Frontend**: `KioskForm` di-refactor jadi dual-mode (`mode: "create" | "edit"`, prop `initialData?: Kiosk`, pola identik T077 `GuruForm`) — mode edit HANYA tampilkan field `nama`+`allowedIp` (dropdown Kampus/Tipe dan section instruksi "Deteksi IP Otomatis" T085 disembunyikan, karena `UpdateKioskDto` backend memang cuma terima 2 field itu — dicek dulu sebelum coding, TIDAK extend DTO tanpa konfirmasi sesuai larangan eksplisit spec), submit ke `PATCH /kiosks/:id`. Tombol Edit (`Pencil`) ditambah di kolom Aksi tabel Daftar Mesin, sebelum tombol URL/Deactivate yang sudah ada (keduanya TIDAK dihapus/diubah). Caption tambahan di field IP mode edit: kalau tap masih ditolak setelah disimpan, cek ulang lewat Deteksi IP Otomatis (menghubungkan T085+T086 jadi 1 alur penyelesaian). Diverifikasi live **pada kiosk PRODUKSI NYATA sumber insiden asli** (`test_laptop`, id 5): `PATCH /kiosks/5 {allowedIp: "192.168.123.2"}` (IP asli yang dilihat server per insiden T085) → 200 tersimpan; tap simulasi dengan `X-Forwarded-For` IP yang sama → LOLOS validasi guard (gagal di DTO `client_uuid` yang tidak relevan, BUKAN di pengecekan IP — membuktikan kasus nyata yang memicu task ini benar-benar teratasi TANPA nonaktifkan+buat kiosk baru); `activity_log` dicek langsung: `kiosk.update` tercatat dengan `snapshot_before`/`snapshot_after` menunjukkan perubahan `allowedIp` persis, `deviceToken` REDACTED di kedua snapshot (tidak bocor). Data kiosk asli DIKEMBALIKAN ke `allowedIp` semula (`10.200.219.194`) setelah verifikasi selesai — tidak ada state produksi yang tertinggal berubah. **Catatan proses:** build+restart sempat tertunda karena pekerjaan paralel T087 (refresh token TTL) mid-edit di `auth.service.ts`/`auth.types.ts` — dikonfirmasi via `git diff` bukan dari perubahan T086, ditunggu sampai selesai baru lanjut verifikasi akhir (pola sama T078/T081/T082 sebelumnya). `tsc` bersih kedua app (setelah T087 selesai: 183/183 test, angka naik dari 175 karena test baru T087), full `pnpm turbo run build` bersih 6/6 (`/kiosk` 10.3kB→10.7kB mencerminkan UI Edit baru). **Blok 18 SEKARANG LENGKAP 2/2 (T085+T086 selesai).**

## 🏗️ Blok 19 — TTL JWT Per-Role: Guru/Kepsek Panjang, Guru Piket Jam 18:00 (BARU, 2026-07-26)
> Diminta user 2026-07-26 setelah diskusi soal masa berlaku token Kiosk/TV (tanpa expiry by design): guru & kepsek ingin TIDAK perlu login ulang tiap buka aplikasi (device sama), guru piket sebaliknya WAJIB auto-logout jam 18:00 setiap hari karena komputer piket dipakai bergantian per-shift. Infrastruktur auto-refresh silent SUDAH LENGKAP di `middleware.ts` (T033) — task ini murni soal DURASI TTL per-role, bukan membangun mekanisme refresh dari nol. **Keputusan final dikonfirmasi user**: "tanpa expiry" = refresh token 1 tahun (bukan literal tanpa `exp`, demi keamanan token dicuri tetap punya batas atas); guru_piket expire TEPAT jam 18:00 hari login, dan kalau login SETELAH jam 18:00 → expire segera/sangat singkat di hari yang sama (BUKAN digeser ke besok — piket yang login telat sengaja dapat sesi nyaris tidak berguna).

- [x] (**dieksekusi+diverifikasi 2026-07-26**) [[Projek/AbsenSI/06-Features/tasks/T087-jwt-ttl-per-role|T087 — API+UI: TTL Refresh Token Per-Role (Guru/Kepsek Panjang, Guru Piket Expire Jam 18:00)]] — konstanta baru `REFRESH_TOKEN_TTL_LONG_SECONDS` (1 tahun) untuk `guru`+`kepsek`, fungsi `computePiketRefreshTtl(now)` (pure function, hitung sisa detik ke jam 18:00 hari ini dari `now`, dipanggil ulang tiap `issueTokenPair()` termasuk saat `refresh()` — refresh di siang hari TIDAK memperpanjang sesi piket melewati jam 18:00 hari itu by construction, tanpa state tambahan) untuk `guru_piket`, dengan grace period 60 detik kalau login/refresh terjadi SETELAH jam 18:00 (bukan digeser ke besok). Role lain (`super_admin`/`card_admin`/`admin_jurnal`) TIDAK disentuh, tetap 7 hari `REFRESH_TOKEN_TTL_SECONDS` — scope literal cuma 2 role yang disebut eksplisit. `TokenPair.expiresIn` ditambahkan sebagai field baru di response login/refresh — backend jadi satu-satunya sumber kebenaran TTL. Constant lama `REFRESH_TOKEN_TTL_SLIDING_SECONDS` (30 hari, dulu kepsek-only) dihapus karena jadi dead code (dikonfirmasi TV Piket pakai mekanisme token statis terpisah, tidak lewat `issueTokenPair()`).
  Frontend: `apps/web/src/lib/session.ts` — `REFRESH_TOKEN_MAX_AGE` konstan diganti fungsi `refreshTokenMaxAge(expiresIn)` (`Math.min(expiresIn, ONE_YEAR_SECONDS)` sebagai batas atas jaga-jaga), dipakai di `login/route.ts` dan `middleware.ts` (silent auto-refresh) — tidak duplikasi logic per-role/jam-18:00 di frontend, cukup pakai `expiresIn` dari backend apa adanya.
  Test baru: `auth.service.spec.ts` — 4 test di describe "T087 — TTL refresh token per-role" (guru/kepsek ~1 tahun, super_admin/card_admin/admin_jurnal tetap 7 hari, guru_piket range plausible dari `login()` & `refresh()`) + 4 test terisolasi di describe top-level `computePiketRefreshTtl` (pagi 09:00 → sisa ke 18:00 tepat; 17:59:30 → sisa 30 detik; tepat 18:00 → grace 60 detik; setelah 18:00 (20:00) → grace 60 detik, bukan digeser ke besok) — dipisah dari test integrasi karena `jest.useFakeTimers`/mock `Date` global merusak `bcrypt`/`jsonwebtoken` internal timer, jadi function murni ini diuji langsung dengan parameter `now` eksplisit tanpa mocking apapun.
  Verifikasi: `tsc --noEmit` bersih (api+web), `nest build`+`next build` sukses, jest 183/183 (13 suites, naik dari 175 baseline). Live curl terhadap server dev: `super_admin` (admin/password123) → `expiresIn: 604800` (7 hari, tidak berubah); `kepsek` → `expiresIn: 31536000` (365 hari tepat); user `guru_piket` disposable dibuat via script sementara, login jam 11:19:27 → `expiresIn: 24032` detik = tepat jam 18:00:00 hari yang sama (dihitung manual, cocok). User test dinonaktifkan (bukan dihapus — FK constraint dari `activity_log.actor_id` mencegah hard delete setelah percobaan login tercatat).

## 🏗️ Blok 20 — Migrasi UUID Tabel Sensitif (TIDAK MENDESAK — tunggu instruksi eksplisit)
> Dibuat 2026-07-26 setelah user khawatir ID sekuensial (1,2,3...) rentan IDOR/enumerasi, beda dari database lama (Laravel) yang pakai UUID. **Audit keamanan menyeluruh dilakukan LEBIH DULU** ke semua endpoint `:id` — hasilnya SEMUA endpoint yang seharusnya scoped (guru ke datanya sendiri, guru_piket ke kampusnya, wali kelas ke kelasnya) SUDAH benar dijaga di level service; endpoint tanpa scoping lain memang desain sengaja (admin global/lintas-kampus), konsisten di seluruh sistem. **Kesimpulan: migrasi UUID TIDAK menutup celah keamanan nyata apapun** — user tetap ingin task ini disiapkan untuk kemudian hari (preferensi konsistensi, bukan alasan keamanan). **JANGAN eksekusi tanpa instruksi eksplisit user, dan jangan prioritaskan di atas task lain yang punya manfaat fungsional nyata.**

- [ ] [[Projek/AbsenSI/06-Features/tasks/T088-uuid-tabel-sensitif|T088 — Schema+API: Migrasi PK Sequential Int → UUID untuk Tabel Sensitif (Student, Teacher, Card)]] — scope TERBATAS 3 model (bukan seluruh 27), strategi dual-column (`uuid` kolom baru, `id` integer tetap ada sebagai internal FK reference, tidak lagi di-expose ke client). TIDAK menyentuh `AttendanceRecord`/`TapEvent` (volume 348rb+ baris, insert-only, tidak ada manfaat). TIDAK mengubah `Card.uid` (nomor fisik kartu RFID, beda dari `Card.id` PK database — jangan tertukar scope). WAJIB dikerjakan di maintenance window (downtime terjadwal), backend+frontend deploy bersamaan.

## 🏗️ Blok 21 — Redesign Tampilan Absensi Kiosk + Watermark Copyright (BARU, 2026-07-26)
> User berikan 4 sketsa desain (`Guru pengajar.svg`, `staf karyawan.svg`, `stanby.svg`, `gambar-100.jpg.jpeg`) 2026-07-26 — sketsa BUKAN untuk ditiru persis (dikonfirmasi user, boleh sesuaikan warna/ukuran ke design system AbsenSI yang sudah ada). Perbedaan Guru vs Staf/Karyawan HANYA label + section "Jadwal Mengajar Hari Ini" (staf tidak ada). Riwayat Datang/Pulang di sketsa = KOLEKTIF (bukan riwayat pribadi), sama seperti `KioskRecentTable` yang SUDAH ADA di `GuruDashboard` — REUSE, jangan bikin ulang. Tampilan personal ini MENGGANTIKAN `FeedbackScreen` overlay yang sudah ada (bukan dashboard baru terpisah). UI Siswa SENGAJA lebih sederhana (foto+nama+jam saja, TANPA riwayat) — instruksi eksplisit user. Backend perlu extend `TapResponse` (field baru: `identifier`/NIY-NISN, `statusKepegawaian`, `jadwalHariIni`) karena data ini belum pernah di-expose sebelumnya, meski sumber datanya (`Teacher.statusKepegawaian`, `Schedule` type=jam_mengajar) sudah ada di skema.

- [x] [[Projek/AbsenSI/06-Features/tasks/T089-redesign-tampilan-absensi-kiosk|T089 — API+UI: Redesign Tampilan Absensi Kiosk (Guru/Staf/Siswa) + Watermark Copyright]] — **SELESAI + diverifikasi live 2026-07-26.**
  - **Backend**: `TapResponse` (`packages/types`) diperluas dengan `identifier` (NISN siswa / NIY guru), `statusKepegawaian` (`"guru" | "karyawan"`, hanya terisi untuk kiosk tipe guru), `jadwalHariIni` (array `{kelas, jamMulai, jamSelesai}` — array kosong = guru tanpa jadwal hari itu, `undefined` = bukan guru). `AttendanceService.buildAcceptedResponse` diubah jadi `async`, query `Schedule` (`type: jam_mengajar`, `teacherId`, `hari`) hanya dijalankan sekali per tap kalau `statusKepegawaian === "guru"` — bukan loop, memanfaatkan index `@@index([teacherId])` yang sudah ada di skema (tidak ada N+1). `student`/`teacher` sudah di-`include` penuh di 3 lokasi query yang ada, jadi `nisn`/`niy`/`statusKepegawaian` tidak butuh query tambahan.
  - **Frontend kiosk**: komponen baru `KioskWatermark` (shared, tahun dinamis via `new Date().getFullYear()`, prop `light` untuk kontras di atas background gelap) dipasang di SEMUA layar — `IdleScreen`, `GuruDashboard`, dan tiap varian `FeedbackScreen` (justLocked, siswa, guru/staf, rejected). `IdleScreen` diredesign — jam diperbesar jadi elemen visual utama (120px → 220px), heading "AbsenSI" mengecil jadi header compact di atasnya. `FeedbackScreen` ditulis ulang total: varian siswa tetap sederhana (foto+nama+jam, tanpa riwayat/NIY/jadwal, sesuai instruksi eksplisit); varian guru/staf jadi 2-kolom — kartu personal kiri (foto besar, label "Guru Pengajar"/"Staf/Karyawan", NIY, jam) + section "Jadwal Mengajar Hari Ini" (hanya render kalau `isGuru`, tampilkan "Tidak ada jadwal mengajar hari ini." kalau array kosong — section TIDAK pernah disembunyikan untuk guru) + kolom kanan riwayat kolektif me-reuse `KioskRecentTable` yang sudah ada di `GuruDashboard` (bukan komponen baru). `justLocked`/rejected dipertahankan (overlay gelap/merah solid), hanya ditambah watermark.
  - **Bug ditemukan+diperbaiki saat verifikasi visual (Playwright)**: `KioskAvatar` fallback icon (`bg-white/20`+`text-white`) invisible di atas kartu putih baru (dulu didesain hanya untuk background gelap solid). Ditambah prop `light` yang switch ke `bg-surface-subtle`+`text-ink-tertiary`; dikonfirmasi ulang via screenshot fallback icon sudah terlihat jelas. Ini murni ditemukan lewat cek visual nyata, bukan review kode — menegaskan kenapa "WAJIB dicoba tampil nyata" di spec task ini penting.
  - **Verifikasi**: `tsc` semua app clean, Jest 183/183 lulus, `pnpm turbo run build` 6/6 sukses (route `/kiosk` 19.2kB→19.8kB). Tap real via curl untuk guru (dengan+tanpa jadwal hari itu), staf, siswa — field `identifier`/`statusKepegawaian`/`jadwalHariIni` terisi benar sesuai tipe. Playwright screenshot mengonfirmasi render guru (dengan panel jadwal), staf (tanpa panel jadwal), siswa (kartu sederhana), justLocked, dan rejected — semua dengan watermark tampil. Ditemukan juga kiosk `test_laptop` (id 5) `allowedIp` tertinggal di state simulasi `192.168.123.2` dari sesi T086 sebelumnya — dikembalikan ke `10.200.219.194` via `PATCH /kiosks/5`. Dev server 3000/3001/3002 dicek sehat (1 proses per port) setelah build+restart final.

## 🏗️ Blok 22 — Hapus Mesin (Delete Kiosk) (BARU, ad-hoc, 2026-07-26)
> Diminta user 2026-07-26: menu Mesin cuma ada Edit+Nonaktifkan, belum ada Hapus permanen. Bukan dari task spec vault manapun — permintaan langsung di chat.

- [x] (**dieksekusi+diverifikasi 2026-07-26**) Tambah `DELETE /kiosks/:id` — **Backend**: `KiosksService.remove(id)` cek dulu `tapEvent.count()`+`attendanceRecord.count()` untuk kiosk itu (dua tabel yang punya FK nullable ke `Kiosk`, keduanya insert-only per ADR-020) — kalau salah satu >0, lempar `BadRequestException` ("sudah punya histori tap, gunakan Nonaktifkan sebagai gantinya") dan TOLAK hapus (hard delete akan gagal FK constraint kalau dipaksa, dan menghapus kiosk berhistori berarti mengorbankan integritas tap_events). Kalau kiosk kosong (baru dibuat/salah input), `prisma.kiosk.delete()` jalan normal. Pola sama persis `KelasService.remove()` (cek `studentCount`/`scheduleCount` sebelum hapus) — reuse konvensi yang sudah ada, bukan bikin pola baru. `@LogActivity({action: "kiosk.delete", targetType: "kiosk", prismaModel: "kiosk", sensitiveFields: ["deviceToken"]})` dipasang di controller (konsisten aturan permanen T067), `@Roles(UserRole.super_admin)` saja sesuai role lain di controller ini.
  **Frontend**: tombol `Trash2` baru di kolom Aksi tabel Daftar Mesin — HANYA muncul kalau `!kiosk.isActive` (kiosk aktif tetap cuma dapat tombol Nonaktifkan, harus dinonaktifkan dulu baru bisa dihapus — mencegah admin tidak sengaja hapus mesin yang masih dipakai). Dialog konfirmasi menampilkan pesan error dari backend apa adanya kalau ditolak (histori tap), supaya admin langsung tahu kenapa gagal tanpa perlu baca log server.
  **Verifikasi live** (curl terhadap dev server, rebuild `nest build`+restart proses `dist/main` supaya endpoint baru kebaca — proses lama masih jalankan build sebelum perubahan ini): kiosk kosong baru (`T-Delete-Test`, id 12, 0 tap) → `DELETE /kiosks/12` → 200, hilang dari `GET /kiosks`, tercatat di `activity_log` (`kiosk.delete`, target_id 12). Kiosk `test_laptop` (id 5, 2 tap_events nyata) → `DELETE /kiosks/5` → 400 ditolak dengan pesan yang benar, kiosk TIDAK terhapus (data produksi aman). `tsc --noEmit` bersih kedua app.

---

## 📌 Catatan Penting

1. **Sebelum mulai coding setiap task:** baca `Ref` yang direferensikan di task file, dan baca ulang bagian relevan di `dashboard-guru-jurnal.md` — spec ini masih berstatus `planning-interview`, bukan `final`, jadi ada baiknya cross-check tidak ada perubahan sejak breakdown ini dibuat.
2. **Geofence hard-block tanpa override (T043)** adalah keputusan sadar user meski berisiko UX (GPS gagal di gedung beton, dst) — JANGAN diam-diam menambahkan jalur override "untuk jaga-jaga", itu perubahan scope yang harus dikonfirmasi ulang ke user kalau ternyata dibutuhkan.
3. **Open questions yang SENGAJA tidak dijawab task ini** (jangan improvisasi sendiri, tunggu keputusan user): batas waktu edit jurnal setelah closed, radius geofence pasti per kampus (admin isi manual via UI kampus — bukan scope batch task ini, kemungkinan perlu task tambahan kecil untuk extend UI master-data kampus), field "Tujuan Pembelajaran" terhubung CP/TP formal atau tidak. **Role Wali Kelas SUDAH final** (lihat Blok 6), bukan open question lagi.
4. **TV Piket ([[Projek/AbsenSI/06-Features/tv-piket|tv-piket.md]])** bergantung ke data dari batch task ini (`teaching_sessions`, `teacher_permits`) — jangan mulai task TV Piket sebelum Blok 0-3 batch ini selesai dan datanya bisa diverifikasi nyata.
5. **T053 (menu Wali Kelas) HANYA tampilkan field `catatan` dari jurnal**, bukan materi/tujuan/tugas — kalau nanti diminta buka akses penuh, itu perubahan kecil (extend query + ubah tampilan), sudah didokumentasikan caranya di spec, JANGAN dikerjakan sekarang tanpa instruksi eksplisit.
16. **WAJIB `@LogActivity` di SETIAP controller/endpoint mutasi BARU** (POST/PATCH/PUT/DELETE yang mengubah data) — ini aturan permanen sejak audit T067 (2026-07-22), bukan cuma untuk task itu saja. Kalau menulis controller baru di task manapun (T038-T066 dan seterusnya), cek dulu apakah decorator ini sudah terpasang sebelum menandai task selesai. Lihat memory permanen `feedback_wajib_log_activity`.
6. **`teacher_permits.bukti_file_path` WAJIB diisi sejak awal (T038/T046)** — beda dari `tugas_file_path` yang nullable. Jangan tertukar keduanya: `bukti_file_path` = alasan izin (surat dokter/nota dinas/dst), `tugas_file_path` = materi pengganti untuk siswa.
7. **Semester (T054) adalah domain `super_admin`, bukan `admin_jurnal`** — meski konsumen utamanya (assign jadwal) ada di dashboard admin_jurnal. Jangan beri admin_jurnal wewenang create/edit/activate semester, hanya read + pilih dari dropdown. **Tapi `block_week_ranges` (isi rentang blok A/B) ADALAH wewenang `admin_jurnal`** — dua tabel beda pemilik, jangan disamakan.
8. **Validasi no-gap/no-overlap di T054 adalah bagian TERPENTING dari seluruh batch semester** — jangan pernah skip atau menyederhanakan validasi ini demi kecepatan implementasi. Kalau validasi ini bocor, `ScheduleResolverService` (T039) bisa menghasilkan data ambigu (>1 hasil untuk 1 tanggal) yang merembet ke seluruh sistem (sesi mengajar, jurnal, rekap).
9. **Audit kritik tambahan (2026-07-22) sudah ditutup di T054:** (a) race condition submit rentang overlap harus dicegah dengan DB transaction+lock, bukan cek-lalu-insert biasa; (b) `PATCH /semesters/:id/activate` WAJIB tolak (409) semester `mode: blok` yang masih ada lubang jadwal — mencegah insiden "semua guru terkunci besok"; (c) `block_week_ranges` tidak boleh diubah/dihapus untuk tanggal hari ini/masa lalu, hanya masa depan; (d) endpoint `upcoming-gaps` WAJIB dipakai sebagai banner peringatan proaktif di layout admin_jurnal (T050), bukan cuma info pasif yang harus dicari manual. Semua ini SUDAH masuk ke T054/T050 versi terkini — jangan implementasi versi yang lebih longgar dari ini.
10. **Trade-off yang disadari, JANGAN "diperbaiki" tanpa instruksi:** hard block lubang jadwal berlaku per-SEMESTER (semua kelas terkunci sekaligus), bukan per-kelas granular — meski ada kelas yang jadwalnya `setiap_minggu` dan sebenarnya tidak terdampak minggu A/B. Ini keputusan sadar demi kesederhanaan 1 sumber kebenaran, lihat "Trade-off yang Disadari" di `dashboard-guru-jurnal.md`.
11. **Sebelum sesi berikutnya mulai eksekusi apapun: pertimbangkan `git add`+`git commit` dulu untuk T038-T054/T056** — semuanya sudah ada di working tree tapi belum ter-commit sama sekali (dicek 2026-07-22). Ini bukan instruksi untuk auto-commit tanpa diminta (tetap ikuti aturan "jangan commit kecuali diminta user"), tapi ini risiko nyata yang perlu diingatkan ke user di awal sesi berikutnya.
12. **MCP tools tersedia untuk sesi berikutnya:** Playwright MCP (`mcp__playwright__*`) untuk screenshot/interaksi browser nyata, MySQL MCP read-only (`mcp__mysql-absensi__mysql_query`) untuk cek state data langsung, dan plugin Impeccable (`/impeccable audit`, `/impeccable critique`, dst) untuk deteksi anti-pola desain otomatis — pakai ketiganya terutama untuk T060.
13. **`DESIGN.md` di root repo dan `06-Features/design-system/*.md` di vault HARUS disinkronkan** — vault adalah sumber kebenaran, `DESIGN.md` di root adalah rangkuman untuk Impeccable. Kalau salah satu diedit, cek yang lain juga perlu update (lihat riwayat 2026-07-22: token status-shipped/processing ditambahkan ke KEDUANYA).
14. **Aturan Sheet+Tabs (T064) berlaku PERMANEN untuk SEMUA form ke depan, tidak cuma T065/T066** — kapan pun ada task baru yang butuh form input >6 field, WAJIB pakai pola Sheet+Tabs dari `03-components.md`, JANGAN buat `Dialog` baru untuk kasus itu. Form ≤6 field sederhana tetap boleh `Dialog` kecil seperti sebelumnya.
15. **Migrasi database lama (Blok 10) dan form modern (Blok 11) SALING TERKAIT tapi independen urutannya** — Blok 11 (T065/T066) depend ke Blok 10 (T061/T063) untuk field baru di backend, tapi Blok 10 tidak depend ke Blok 11. Bisa kerjakan Blok 10 duluan tanpa menunggu Blok 11, tapi TIDAK sebaliknya.

## 🔗 Lihat Juga
- [[Projek/AbsenSI/06-Features/dashboard-guru-jurnal|dashboard-guru-jurnal.md]] — spec fitur lengkap
- [[Projek/AbsenSI/13-Backlog|13-Backlog]] — posisi fitur ini dalam roadmap Fase 2
- [[Projek/AbsenSI/TASKS-FASE-1|TASKS-FASE-1]] — pola/format breakdown task yang diikuti
